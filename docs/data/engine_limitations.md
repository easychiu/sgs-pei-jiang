# 引擎已知限制清單

供未來稽核/資料維護引用同一基準。彙整自歷批(批2~批20)資料修正時發現、且短期內不打算
(或無法在不動 `sgz.py`/`engine.js` 引擎程式碼的前提下)修的結構性缺口。所有這裡列出的限制,
在 `docs/data/tactic_corrections.json` / `docs/data/tactics_parsed.json` 對應戰法上都應該
有 `_todo`/`_note`/`_approx` 揭露, 而不是靜默套用近似值。`lint_tactics.py` 的豁免規則
(`has_disclosure`)正是依賴這個「有揭露 = 已知且可接受」的約定。

新增限制時, 請同時: (1) 在此檔案加一節, (2) 在對應戰法的 correction/parsed 條目補
`_todo`/`_approx`, (3) 若該限制是規律性的(影響一整類戰法), 考慮加進 `lint_tactics.py`
的豁免白名單或新增規則。

## 維護規約: `_note`/`_todo` 聲稱的數值必須與資料實際值一致(批23新增, `lint_tactics.py` R14)

**背景**: 批23全庫核對時發現多筆「stale 揭露」——`_note`/`_todo` 文字曾經正確描述某個
`coef`/`dur`/`rate` 應該是多少, 但後續資料改動(如 correction 的 `set` 被拿掉、或新一輪
`reparse`覆寫)沒有同步更新揭露文字, 導致文字聲稱的數值與 `tactics_parsed.json` 實際欄位
矛盾(如神機妙算 `_todo` 曾聲稱「coef=1.28取主將分支值」但 `correction` 從未真正 `set`
`coef`, 實際欄位停留在舊值 1.0; 錦帆軍同理聲稱 coef=1.28 但實際仍是 0.64)。這類矛盾比
「完全沒揭露」更危險——維護者讀到 `_note` 會誤以為資料已經是對的, 不會再去複查實際欄位。

**規約**: 修改任何戰法的 `coef`/`dur`/`rate`/`n`/`nMax` 時, 若對應的 `_note`/`_todo` 文字
提到這些欄位的具體數字, 必須同步檢查文字是否仍與新值一致。歷史敘述(如「批X: 原coef=Y
改為Z」「移除舊版heal(coef=W)」)是合法且應保留的, 但「宣稱目前狀態是 X」的斷言句必須與
`set`/`effects` 實際寫入的值相符。

**自動檢查**: `lint_tactics.py` R14 掃描 `_note`/`_todo`/`_note2`/`_note_self`/`_approx`
文字裡 `coef=X`/`dur=X`/`rate=X` 這類精確斷言句型, 與該戰法(頂層)或該效果(effects[i])的
實際欄位值比對, 不一致則報違規。刻意排除 `n`/`nMax`(頂層 `n` 與效果級 `e.n` 是不同概念但
常共用「n=X」措辭, 比對會系統性誤判, 見批23 R14 設計註解), 也用歷史敘述關鍵字(原/舊/之前/
過去/曾/取代/先前/移除/刪除/歸零/誤用等)過濾掉合法的「敘述舊值」句型, 只抓「未被歷史詞
修飾的當前狀態斷言」, 維持低誤報優先原則。

## 1. 治療(heal)只補「最殘一人」, 無法指定/群體同時治療多人

`apply_effects()` 的 `heal_only` 常駐通道(見 `sgz.py` 的 `k=="heal"` 分支)固定選
`min(alive_and_not_healblocked, key=troop)` 這一人, 不支援「治療我軍群體N人」或「治療我軍
全體」——所有 heal 效果無論原文寫「單體/群體2人/全體」, 實際都只會治療兵力最低的那一人。
這是全庫最大宗的近似來源之一(heal 類戰法全部受影響)。

## 2. 無 cooldown(冷卻)原語

原文常見「每N回合限發動一次」/「發動後進入1回合冷卻」等機制, 引擎的 `rate` 是逐回合獨立
擲骰(無記憶性), 無法表達「發動過一次後接下來M回合不能再發動」。既有近似慣例: 用期望值折算
(EV, expected value)把冷卻的稀釋效果算進 `rate` 裡, 如 `rate_effective = p/(1+p)`(1回合冷卻
且原機率p的情形, 見 臨機制勝 correction 的 `_note`)。

## 3. 無效果轉移(transfer)/治療轉移原語

移花接木類「將受到治療的一部分轉移到自己」機制沒有專屬原語, 只能用近似(如把「轉移量」折
算成一個獨立的 heal 效果, 損失了「轉移」本身要從對方身上扣除的雙向語意)。

## 4. 效果級機率(`e.rate`)折算 —— 批23 A4 已解決: 通用 `e.rate` 判定(非僅 `counter`/`dodge`)

**歷史限制(批22前)**: 效果級機率(prob)欄位僅 `counter`/`dodge` 兩種 k 原生支援 `prob` 欄位,
且 `delayedEq`/`heal`(僅 `e.when.on` 反應式路徑)才會依情境額外讀 `rate`/`prob`。大多數效果
種類(`amp`/`mitig`/`stat`/`stun` 等)一旦被 `apply_effects()` 套用就是100%發生, 沒有「這個
效果本身有X%機率生效」的通用欄位。

**批23 A4 修復**: `apply_effects()`(`engine.js`/`sgz.py`)現在對**任何** `k` 種類的效果, 只要
帶 `e.rate` 欄位, 套用前都會先擲骰判定(`rnd() < e.rate`, 未帶 `e.rate` 的效果維持無條件套用,
向後相容)。呼叫端若已自行讀取並擲骰過同一個 `e.rate`(如 `onHit`/`delayedEq` 的合成單效果
呼叫), 傳 `opt.rateChecked`(JS)/`rate_checked`(Python) 旗標避免重複擲骰(機率被平方, 造成
低估)。原文「有25%→50%機率獲得XX狀態」這種**單一效果的局部機率**現在可以精確用 `e.rate`
表達(套用時真實擲骰, 比 EV 折算更接近真實方差), 不需要再折算成期望值。

**仍維持 EV 折算的情形**(非本次修復範圍, 沿用既有慣例): 原文機率描述無法精確對應到單一
`e.rate` 判定點的情形(如同一戰法內多個效果共用同一句機率描述、或機率本身依動態條件縮放),
仍可用 `_approx:"prob-ev"` 折算成期望值, 見解煩衛(heal coef 已折算 60%機率×72%治療率=0.432)、
魚鱗陣(mitig 用機率值本身近似減傷比例)。對於本質上是 binary(全有全無)的效果(如 `insight`
全免疫控制)且無法明確定位單一 `e.rate` 擲骰點時, EV 折算仍會嚴重失真, 應保持誠實 `_todo`,
不強行折算。

## 5. 傷兵池(wounded pool)機制

battle 18 新增: 受到的傷害依「當時回合數」轉換成「可救援(計入傷兵池, 治療只能回這部分)」
vs「不可救援(直接陣亡量, 治療無法挽回)」兩部分, 轉換率隨回合遞減
(`WOUNDED_RATES = [0.90,0.90,0.90,0.80,0.80,0.80,0.675,0.675]`, 見 `sgz.py`)。這是遊戲內
實測機制, 已正確建模, 但意味著「治療率X%」不能簡單理解成「回復X%×基礎兵力」——後期回合的
實際回復量會因傷兵池餘量不足而被削弱, 純數值比對(不跑模擬)容易誤判治療類戰法強度。

## 6. 近似慣例一覽(EV折算/群控隨機/點數mult豁免清單等)

以下是歷批已建立、且被視為「可接受的標準近似」的清單, 遇到同類情形應優先沿用既有慣例,
不要各自發明新近似方式(維持全庫一致性)。

### 6.1 EV(期望值)折算
「每回合X%機率觸發」若不能用 `when`(rounds/from/until/parity/every)精確表達回合窗口
(通常是因為同一戰法內還有其他效果需要在不同時間窗常駐生效, 若都套用 `t["when"]` 會把
不該延後的效果也延後, 見下方6.4), 改用 `rate`(戰法整體發動率)或效果 `val`/`coef` 直接
乘上機率折算成穩態期望值。

### 6.2 群體控制隨機選標
「群體N人」的控制效果(stun/silence/disarm/taunt/chaos, 即 `ctrl_k` 集合)用
`pick_targets(enemies, cnt)` 隨機不重複選 cnt 個目標, 不支援按條件(如「兵力最高的N人」)
篩選後再隨機——只有 `targetSel`(單體, 見下方6.5) 支援條件選標, 群體控制只能全隨機。

### 6.3 點數(flat)vs百分比(mult) —— 批12 ModeA 修正基準; `lint_tactics.py` R21 防線(批26新增)
原文常見「統率提升X→Y點」這種**固定點數**加成, 早期整批重解誤用 `stat.mult`(百分比乘算)
表示, 批12 ModeA 已系統性修正為 `stat.add`(固定值平加)。日後若發現任何戰法仍是
「點」語意卻用了 `mult`, 應視為同一類回歸(bug), 直接修正, 不需要另外近似。

**批26發現**: 全庫盲測病因排查抓到2筆此類回歸(奮矛英姿 effects[0]/魚鱗陣 effects[0]),
其中奮矛英姿更棘手——頂層 `_note` 已聲稱「stat.mult→stat.add修正」完成, 但該戰法內**另一個**
stat效果(不同的目標/屬性, 同戰法內效果級的局部修正未覆蓋到全部效果段)仍留著未修的 `mult`,
形成「聲稱已修但實際只修了一半」的 stale 局面, 比完全沒修更容易被誤認為已解決。已修正兩筆
(改用 `stat.add`, 見 `tactic_corrections.json`)。`lint_tactics.py` R21 已將此類回歸制度化為
自動巡檢規則: 偵測「該屬性緊鄰窗口內完全找不到%符號」(裸點數語意, 兩種措辭皆抓: 「動詞N點
屬性」動詞在前, 或「屬性 動詞 N」且緊鄰無單位符號)但對應 `stat` 效果仍用 `mult` 而非 `add`
的情形, 全庫掃描零違規視為合格基準。

### 6.4 e.when 的「母戰法無 t.when」限制
效果級 `e.when` 只在**母戰法本身沒有 `t.when`** 的情形下, 由 `fight()` 回合迴圈的通用
掃描(非 heal 種類, 見 `sgz.py` 1180-1196行)在符合窗口時套用一次。若母戰法 `t.when` 已存在
(如某戰法整體只在第N回合觸發), 該戰法內其餘效果(即使自己也想要不同窗口)的 `e.when` 會被
`skip_when_effects` 邏輯忽略, 因為準備階段套用邏輯是「先看 t.when, 沒有才落到效果自己的
e.when」, 兩者不能同時套用不同窗口在同一戰法的不同效果上(這正是 heal 需要獨立通道
`heal_only` 的原因——heal 的 e.when 優先於 t.when 判斷, 其餘效果種類目前沒有這個優先權)。
這是「一個戰法內有多個不同時間窗效果」時常見的取捨來源, 遇到時通常用 EV 折算(6.1)或
選擇性只精確表達其中一個窗口、其餘維持常駐近似。

### 6.5 targetSel(指定選標準則) vs 群體隨機(ctrl_k)
`targetSel` 只支援**單體**依準則挑選(`minTroop`/`maxForce`/`minIntel`/`maxIntel`/
`minCommand`/`mostDamaged`, 見 `sgz.py` `TARGETSEL_KEY`), 且只能選我方或敵方整體池,
無法表達「隊伍固定 index 位置」(如「敵軍主將」=敵方隊伍 index 0 這種**結構性位置**,
不是「兵力/屬性最高最低」這種**動態計算的準則**)。`extraHits` 段有 `who:"enemyLeader"`
可以固定選敵方 index 0, 但**主效果(`effects`)層級沒有對應的 `who` 值**——這是本批(批20)
守而必固「敵軍主將」被迫近似成「隨機挑1名敵軍」的根因, 詳見下方第7節。

### 6.6 「受X或Y最高一項影響」/「受自身最高屬性影響」的 scale 近似
`scale` 只支援單一固定屬性(`intel`/`force`/`command`/`speed`/`charm`), 原文若寫「受武力
或智力最高一項影響」(如 解煩衛/益其金鼓/魚鱗陣)或「受自身最高屬性影響」(如 扶危定傾的
首段減傷), 無法動態判斷「哪個屬性當下比較高」。近似慣例: 固定取其中一個具代表性的屬性
(通常武將卡面主屬性偏向的那一項, 如 force), 或保守不加 scale(如扶危定傾的5屬性最高值情形,
候選過多且無主次之分, 不強加單一猜測)。

### 6.7 「疊加/可疊加」的疊加次數 vs push_add 同源刷新
引擎的 `push_add`/`push_mod`/`push_stat_add` 對「同 kind + 同 src(戰法名)」的效果採
**刷新而非疊加**(見 `sgz.py` `push_add` docstring)。原文寫「可疊加N次」的戰法(如一力拒守
「最多疊加2次」)若同一戰法重複觸發, 引擎只會刷新成同一份效果值, 不會真的疊加到N倍。這是
已知的低估來源, 目前沒有通用解法(需要為疊加類效果新增獨立的「疊加層數」狀態機制, 類似
`stack` 原語但要能綁定到任意 k, 超出現有 `stack`(僅支援自身每回合固定遞增1層增傷)的適用
範圍)。

## 7. 引擎已知限制: 效果級選標無「敵軍主將(隊伍固定 index)」原語

**發現於**: 批20, 守而必固「戰鬥開始時，嘲諷敵軍主將」。

`taunt`(嘲諷)效果的 `who` 只支援 `self`/`leader`/`subs`/`enemy`/`ally` 這幾種集合式選標
(`enemy` = 敵方全體或依 `ctrl_k` 邏輯隨機選N人), 沒有對應 `extraHits` 段 `who:"enemyLeader"`
的「固定選敵方隊伍 index 0(主將位)」。當前用 `n=1`(戰法頂層) 讓 `taunt` 的群體隨機選標邏輯
退化成「隨機挑1名敵軍」, 這比精確的「固定敵方主將」更寬鬆錯位(可能命中副將而非主將)。

**同類受影響戰法**: 任何原文明確寫「敵軍主將」/「我方主將」作為主效果目標、但目標不是靠
屬性動態計算(那種可以用 `targetSel` 解決, 見6.5)而是純粹「隊伍固定位置」的戰法, 都有這個
缺口(如果本批之外還有其他戰法用到, 應一併補上此節的揭露慣例)。

**修法方向(供未來引擎端擴充參考, 本批不動引擎)**: 在 `sgz.py` 的 `apply_effects()` 主
`who` 判斷式(886-913行)新增 `who=="enemyLeader"`/`who=="allyLeader"` 分支, 邏輯與
`extraHits` 段既有的 `enemyLeader` 完全相同(`dests=[foes[0]] if foes and foes[0].alive
else []`), 讓效果層級也能精確表達「固定隊伍主將位」這個目標, 而不必依賴頂層 `n`/群體隨機
近似。

## 8. ~~引擎已知限制: 反應式觸發(`when.on`)路徑不支援 `choices`(擇一分支)~~ —— 批27 C 已解決

**發現於**: 批20, 魅惑「有22.5%→45%機率使攻擊者進入混亂/計窮/虛弱狀態的一種」(三選一)。

~~`choices`(批16原語, 按權重隨機選一組效果套用)只在 `fight()` 回合迴圈的主動/指揮/被動常駐
輪詢派發路徑被讀取, 該路徑明確排除 `when.on` 為真的戰法, 反應式戰法完全走獨立的 `on_hit()`
事件觸發路徑, 該路徑只讀 `t["coef"]`/`t["effects"]`/`t.get("extraHits")`, 從未讀取
`t.get("choices")`。~~

**批27 C 修復**: `on_hit()`(`sgz.py`/`onHit`, `engine.js`)內, `hit_flags` 去重判定通過、
擲骰命中 `t0["rate"]` 之後、實際讀取 `coef`/`extraHits`/`effects` 之前, 新增
`t = dict(t0, **pick_choice(t0["choices"])) if t0.get("choices") else t0`(比照回合迴圈
主動/指揮/被動常駐派發路徑既有邏輯), 用派發後的合成視圖 `t` 執行後續 `coef`/`extraHits`/
`effects`, `t0` 保留供 `hit_flags`/`round_ok` 等以物件本身為鍵的邏輯維持穩定(不受選分支影響,
與回合迴圈既有的 `t0` vs `t` 慣例一致)。魅惑已用此能力重建三選一(見
`tactic_corrections.json`, `choices` 三分支: chaos/silence/amp(-1) 均分權重), 驗證: 批27
新增的 `sgz.py` self-check 79/80(`on_hit_choices_test` + 大量取樣統計三分支皆可抽中)。

**同類戰法**: 任何 `type` 為 `passive`/`command`/`active` 且帶 `when.on` 的「N選一」反應式
戰法, 現在都可以用 `choices` 精確表達多選一, 不再需要「固定選其中一種效果+揭露另外N-1種
未建模」的近似(全庫掃描確認除魅惑外, 目前無其他戰法同時具備 `when.on` 反應式+多選一措辭,
此缺口類別已絕跡, 非只修代表案例)。

## 9. 引擎已知限制: 觸發機率的「按施放者身份條件縮放」無判斷點 —— 批26新增 `ifLeader`(布林條件閘門), 「主將身份」子情形已解決; 性別/陣營等其餘身份條件仍缺口

**發現於**: 批20, 魅惑「若自身為女性，觸發機率額外受智力影響」。

`Unit.g.gender` 資料確實存在(`General.gender`, 已用於緣分 `apt_add` 條件判斷, 見 `sgz.py`
250行), 但 `round_ok()`/`apply_effects()` 的 `when`/`scale` 機制只支援「回合窗口條件」
(rounds/from/until/parity/every/hpBelow/hpAbove) 與「固定屬性縮放」(scale:"intel"等),
沒有「依施放者的某個離散身份屬性(性別/陣營/兵種適性等級)條件切換觸發機率是否額外縮放」這種
判斷點。這與6.5(targetSel 無法選隊伍固定位置)、批6/批7跳過的「主將」概念缺口(見
`reparse_effects.py` CHARGEUP_ADD/RATE_SCALE_PLAN 註解, 三勢陣/十二奇策等)同屬一類
「引擎缺乏施放者條件分支原語」的問題, 只是條件種類不同(性別 vs 隊伍位置 vs 陣營)。

**批26修復(部分)**: 「隊伍位置(主將/副將)」這個特定子情形已透過新增的效果級 `e.ifLeader`
布林旗標解決(`apply_effects()` 在 `e.rate` 判定之後、`k` 分派之前新增 `if e.get("ifLeader")
and not (allies and allies[0] is caster): continue` 閘門, 見 `sgz.py`/`engine.js`)——原文
「自身為主將時，額外XX」這類措辭現在可以精確表達為「該效果段只在 caster 是 allies[0](隊伍
主將)時才套用」, 不再需要「無條件套用給任何施放者」或「完全不建模」兩種近似, 已落地於
南蠻渠魁/江東小霸王/酒池肉林/天下無雙等戰法(見 `tactic_corrections.json` 對應條目)。

**仍未解決**: 「性別」「陣營」「兵種適性等級」等**非隊伍位置**的身份屬性條件判斷仍是缺口
(`ifLeader` 只判斷「是否為 allies[0]」這一種布林條件, 不是通用的「依任意身份屬性切換」框架)。
魅惑「若自身為女性」的機率縮放仍需要在 rate 擲骰前(`sgz.py` `fire = random.random() <
t0["rate"] + ...` 一類位置)插入依 `caster.g.gender`/`caster.g.faction` 等條件判斷是否套用
額外 `scale` 的分支, 屬引擎邏輯擴充, 非資料層可迂迴解決, 且與 `ifLeader`(單純布林開關,
無縮放語意)是不同的機制形狀(這裡要的是「縮放係數依身份切換」，不是「效果套用與否依身份切換」)。

## 11. 「抵禦(格擋)」單次防禦機制 —— 批22新增 `block` 原語(次數型格擋), 部分場景已解決

**發現於**: 揮兵謀勝、魚鱗陣等「受到傷害時有X%機率獲得1次抵禦(完全格擋當次傷害)」。

批22新增 `block` 原語(`k:"block"`, 欄位 `val`/`times`, 見 `sgz.py`/`engine.js` 的
`push_block`/`consume_block`/`hit()`): `val`(1.0=完全格擋當次傷害, 0.x=部分減傷如
「警戒」的0.4基礎值受智力縮放)、`times`(剩餘次數, 每次受擊消耗1次, 用光即失效)。
判定順序 `dodge → block → shield → 傷害`(見 `hit()`)。同源(同一戰法名)再次施加時
**疊加次數**(`push_block` 內部處理, 不同於 `push_add`/`push_mod` 的「同源刷新覆蓋」
慣例), 貼合戰報「目前抵禦總次數為N」的用語。`dispel(buffs)` 會清除格擋層(視為防禦性
增益, 同 `shield` 慣例)。

**已用 `block` 精確重建的場景**(戰報/官方文字明確給出「N次抵禦/警戒」, 且是**準備階段
或觸發當下一次性授予固定次數**, 非「每回合持續機率補充」): 靈動(裝備, 1次)、槍舞如風
(2次)、折衝禦侮(主將2次)、白衣渡江(首回合1次)、勇者得前(1次)、機鑑先識(準備階段固定
2次「警戒」, val=0.4 scale:intel)、赴湯蹈火(中段回合多層疊加, 官方文字無精確層數,
`times:5` 為 `_est` 粗估)。

**~~仍維持 `mitig` EV折算近似、未改用 `block` 的場景~~ —— 批30 A 已解決**: 揮兵謀勝
(「每回合35%→70%機率獲得1次抵禦」)、魚鱗陣(「前3回合每回合12.5%→25%機率獲得1次
抵禦」)、枕戈坐甲(兵書, 「戰鬥第2-6回合每回合25%機率獲得1次抵禦」)。這些是**持續性
機率補充**(每回合都重新擲骰判定是否再拿到一次格擋機會), 過去 `block` 原語本身只是
「已擁有的格擋次數」的計次器, 不解決「如何逐回合重新判定是否新增格擋次數」這個更上層
的問題。批30 A 新增效果級 `e.everyRound`(見下方第26節)通用逐回合重擲通道後, 三筆已
全數改用 `block`+`everyRound` 精確重建(取代 `mitig` EV折算), 見對應 correction/
bingshu_parsed 條目。

**同類受影響戰法**: 任何原文寫「每回合X%機率獲得N次抵禦/警戒」(持續性機率補充)的戰法,
現在都應優先評估用 `block`+`e.everyRound`(批30新增, 見第26節)精確表達, 不再需要
EV折算近似(除非該效果本身另有無法用 `everyRound` 表達的複合限制, 如依動態條件縮放
機率等, 應個別誠實揭露)。

**批25 R20修正**: 魚鱗陣的 `mitig` 效果 `_note` 曾誤稱「引擎無單次格擋原語」(寫於批22
`block` 原語問世前, 之後未同步更新措辭)——已改寫為準確描述「`block` 原語存在, 但缺少
非heal效果的逐回合機率重擲機制, 故此場景仍不適用」, 避免維護者誤以為 `block` 本身
不存在而重複調查同一個已解決的問題。`lint_tactics.py` R20 已將此類「stale能力聲明」
制度化為自動巡檢規則(見附錄後方 R16-R20 說明)。

## 12. 「依目標(而非施放者)當下狀態縮放」無對應 scale 原語

**發現於**: 批21, 鴟苕鳳姿「普通攻擊傷害提高40%→80%，受目標損失兵力影響」。

`scale` 機制(`sv_val`/`sv_add`/`scale_of()`, 見 `sgz.py`)只支援「施放者(caster)自身當下
固定屬性」縮放(`intel`/`force`/`command`/`speed`/`charm`), 縮放函式簽名固定是
`scale_of(caster, e["scale"])`——沒有「依目標(target)當下某個動態狀態(如已損失兵力
百分比)縮放」的路徑。原文「受目標損失兵力影響」的縮放基準是**目標**逐次攻擊前才知道
的兵力損耗比例(隨戰鬥推進動態變化), 與現有 `scale` 完全是不同的一個維度(施放者固定
屬性 vs 目標動態戰鬥狀態), 現況只能固定取滿級值近似(可能高估早期回合、低估目標接近
陣亡時的真實加成)。

**同類受影響戰法**: 任何原文寫「受目標XX影響」(而非「受自身XX影響」)的動態縮放語意,
都有此缺口, 應在 `_todo` 誠實揭露, 不要誤用現有 `scale` 欄位(它只會被解讀成施放者
屬性, 套用後語意完全跑掉)。

## 13. 普通攻擊(pick_target/pick_target_chaos)不讀 targetSel/lockTarget

**發現於**: 批21, 鴟苕鳳姿「普通攻擊...且鎖定敵方兵力最低單體」。

`targetSel`(依準則選標, 見6.5節) 與 `lockTarget`(首次選定後鎖定沿用) 都只在**戰法自身
的 coef 段/effects 段/extraHits 段**生效(`sgz.py` 主 coef 段的 `t.get("targetSel")`
判斷式, `apply_effects()` 內 `e.get("targetSel")` 判斷式, `fire_extra_hits()` 內
`eh.get("targetSel")` 判斷式)——單位的**普通攻擊**(每回合固定執行, 走
`pick_target_chaos()` → `pick_target()`)完全不查詢任何戰法的 `targetSel`/`lockTarget`
欄位, 只會隨機選敵(唯一例外是 `taunt_by` 嘲諷鎖定)。任何原文要求「普通攻擊指定/鎖定
XX目標」(而非「戰法造成傷害指定目標」)的被動/自帶效果, 現有原語都無法讓普攻本身改變
目標選擇邏輯, 只能誠實揭露「普攻仍隨機選敵, 未鎖定至指定目標」。

**修法方向(供未來引擎端擴充參考, 本批不動引擎)**: 在 `pick_target_chaos()`/
`pick_target()` 呼叫處新增「若攻擊者身上有 passive 宣告 `normalAttackTargetSel`
之類的欄位, 優先依準則選標, 否則退回隨機」的分支, 需要在 `Unit.__init__` 預先掃描
自身 passive 戰法收集此類欄位(仿照現有 `on_hit_tacs` 預篩機制)。

## 14. 效果級 `e.when` 不支援 `hpBelow`/`hpAbove`(只有戰法級 `t.when` 支援)

**發現於**: 批21 重新核對義心昭烈(批17已有 `_todo` 記載相關取捨, 本批進一步查證根因)。

`when.hpBelow`/`when.hpAbove` 的實際判斷函式是 `hp_ok(t, u)`(`sgz.py` 677行), 只在
「母戰法帶 `t["when"]`」的那條回合迴圈掃描路徑(1140-1152行)才會被呼叫。效果級 `e.when`
走的是另一條獨立路徑(「母戰法無 `t.when`」時的通用掃描, 1180-1196行, 見6.4節), 但那條
路徑呼叫的是 `round_ok({"when": e["when"]}, rnd)`, 而 `round_ok()`(657-674行)完全不
檢查 `hpBelow`/`hpAbove` 這兩個鍵——對一個只含 `hpBelow` 的 `when` 物件, `round_ok()`
會直接回傳 `True`(視同無條件通過)。也就是說: 若在 `e.when` 裡塞 `hpBelow`, 該效果會在
**準備階段/第1回合**就無條件套用, 與「等到兵力低於門檻才觸發」的原意完全相反、且比
「誠實揭露未建模」更危險(同8節「choices 在反應式路徑形同虛設」的同類陷阱——資料層
看起來已表達了條件, 引擎實際上完全沒有檢查該條件)。

**後果**: `hpBelow`/`hpAbove` 目前只能用在「戰法級 `t.when`」(且該戰法本身沒有其他
需要不同時間窗的效果, 見6.4節的取捨), 效果級用法目前完全不可用, 資料維護者不應嘗試
用 `e.when.hpBelow` 繞過6.4節提到的 `t.when` 連坐限制——這條路走不通(不是尚未測試,
是已驗證會產生錯誤行為)。

**修法方向(供未來引擎端擴充參考, 本批不動引擎)**: 在 `round_ok()` 內比照 `hp_ok()`
補上 `hpBelow`/`hpAbove` 判斷分支(需要傳入 `u` 參數, 目前 `round_ok(t, r)` 簽名沒有
單位參數, 呼叫端 `round_ok({"when": e["when"]}, rnd)` 也需要一併改成傳入 `u`)。

## 15. `on_hit()` 反應式觸發只認「受擊者自己」, 無法表達「隊友受擊時, 我方觸發」

**發現於**: 批21, 騎虎難下「當除自己之外的友軍受到普通攻擊時，有20%→35%機率...」。

`on_hit(dst, src, is_normal)`(`sgz.py` 1103行) 是「單位 `dst` 被 `src` 攻擊時」的事件
鉤子, 只會掃描 **`dst`(受擊者自己)** 身上帶 `when.on` 的戰法(`dst.on_hit_tacs`)。
它沒有「廣播給隊友」的機制——若某戰法要表達「隊友(非自己)受到攻擊時, 自己觸發反應」
這種第三方旁觀語意, 現有 `on_hit()` 完全無法掛載(掛在被打的人身上不對, 那個人不是
效果的施放者; 掛在效果施放者自己身上也不對, 因為施放者沒有被打, `on_hit()` 根本不會
呼叫到他)。目前只能改用「常駐 coef 擲骰」近似(如騎虎難下改用前, 該戰法被當成
「自己每回合對敵軍發動攻擊」, 與原文『友軍受擊時』的觸發者/時機完全對不上), 或至少
改用 `when.on:"attacked"` 骨架(雖然仍不會被引擎正確觸發於「隊友受擊」情境, 但至少
資料語意上更接近, 且為未來引擎擴充預留掛載點)。

**修法方向(供未來引擎端擴充參考, 本批不動引擎)**: 需要在 `hit()` 結算處, 除了呼叫
被攻擊者 `dst` 的 `on_hit` 外, 額外掃描 `dst` 隊友身上是否有
`when.on:"allyAttacked"` 一類的新事件類型並觸發, 屬於新事件廣播機制, 非資料層可解。

## 16. `mitig`/`amp` 的 `dmgType`(兵刃/謀略)過濾欄位 —— 批24已新增原語, 批25全庫清剿stale殘留

**發現於**: 批21, 暫避其鋒「我軍智力最高的武將受到的兵刃傷害降低X%」+「我軍武力最高的
武將受到的謀略傷害降低X%」(兩個不同目標各自只减免對應的一種傷害類型)。

**批24修復**: `mitig`/`amp` 效果物件新增 `dmgType: "phys"|"intel"` 選填欄位,
`damage()`/`hit()` 結算減傷/增傷時依 `kind` 比對過濾, 無此欄位則維持「全類型生效」
(向後相容)。

**批25 R19/R20 全庫清剿**: 批24新增原語後, 全庫仍有 21 筆戰法(刀出如霆/古之惡來/
圍師必闕/威震華夏/將門虎女/搦戰群雄/校勝帷幄/梳妝擲戟/橫戈躍馬×2/水淹七軍/江天長焰/
沉沙決水/焰逐風飛/苦肉計/藤甲兵/誘敵深入/錦帆百翎/才器過人/落雷/猛擊)的單一傷害類型
措辭未補上 `dmgType`(R19: 原語可用未套用), 另有 3 筆(丹陽兵/奇計良謀/箕形陣)的
`_note`/`_todo` 仍聲稱「引擎不支援傷害類型過濾」等 dmgType 落地前的舊近似說明, 已stale
(R20: stale能力聲明)——已於批25逐筆補上 `dmgType` 並重寫過時措辭(奇計良謀額外用
`targetSel:"maxForce"/"maxIntel"` 拆成兩條精確表達的獨立效果, 取代舊有「完全重複合併
成一條」的誤判; 箕形陣因 `who:"subs"` 選標精度限制暫維持不分型近似, 已改寫措辭避免
再度誤導維護者以為 dmgType 原語本身不存在)。`lint_tactics.py` R19/R20 已制度化此類巡檢,
未來新戰法若有同類缺口會被自動抓出。

## 17. `targetSel` 無 `maxTroop`(兵力最高)準則, 只有 `minTroop`

**發現於**: 批21, 定謀貴決「使敵軍兵力最高的武將嘲諷我軍全體」。

`TARGETSEL_KEY`(`sgz.py` 710-714行)目前支援 `minTroop`/`maxForce`/`minIntel`/
`maxIntel`/`minCommand`/`mostDamaged` 六種準則, 其中 `mostDamaged` 的鍵函式是
`lambda u: u.troop` 且被歸入 `TARGETSEL_MIN` 集合(故 `pick_by_criterion` 對它用
`min()`), 語意等同 `minTroop`(兵力最低=受損最重)——**没有任何準則對應「兵力最高」
(`max(troop)`)**。本批曾誤以為 `mostDamaged` 可以表達「兵力最高」(望文生義誤解,
「損傷最多」被誤讀成「兵力數值本身」的排序方向), 實際套用後方向恰好相反(會精確選中
兵力最低而非最高的敵方單位), 已在核對階段發現並撤回, 改為 `_note` 誠實揭露維持現況
近似(定謀貴決 amp 效果不掛 targetSel, 依舊走預設隨機/群體選標)。

**同類受影響戰法**: 任何原文明確要求「兵力最高的敵軍/我軍」作為選標準則(而非「兵力
最低/最殘」)的戰法, 目前都無法用 targetSel 精確表達, 應誠實揭露維持近似, 不要嘗試
借用 `mostDamaged` 或其他現有準則替代(方向必定錯誤)。

**修法方向(供未來引擎端擴充參考, 本批不動引擎)**: 在 `TARGETSEL_KEY` 新增
`"maxTroop": lambda u: u.troop`(不加入 `TARGETSEL_MIN`, 使 `pick_by_criterion` 對它
用 `max()`), 同時建議把 `mostDamaged` 從語意混淆的別名改成明確只保留 `minTroop`
一種寫法(或至少在文件/命名上更清楚區分, 避免未來又發生同樣的方向誤判)。

## 18a. ~~`choices`(擇一分支) 對 `command`/`passive` 型戰法完全不生效(僅 `active` 型可用)~~ —— 批27 B 已解決

**發現於**: 批24, 核對桃園結義(治療我軍單體/謀略傷害智力最低敵單體/兵刃傷害統率最低敵單體
三選一, `type:"command"`)能否用 `choices` 精確表達三選一時發現。

~~`choices` 的派發判定對 `command`/`passive` 型戰法確實會擲骰決定 `fire` 並呼叫
`pickChoice(t0.choices)` 解析出本次觸發實際使用的分支 `t`——但緊接著套用 `applyEffects(...)`
的呼叫只在 `if (t.type === "active")` 條件下才執行, `command`/`passive` 型別完全跳過這一步、
不會把已經抽出的分支效果套用到任何人身上, 而 `command`/`passive` 型戰法真正的效果落地
管道 `applyPassives()` 讀的又是從未經過 `pickChoice` 解析的 `t0`——`choices` 陣列在任何
路徑下都不會被讀取執行。~~

**批27 B 修復**: `fight()` 主迴圈 `fire=true` 分支內, 原本 `if (t.type === "active")
applyEffects(...)` 之後新增 `else if (t0.choices) applyEffects(u, ..., t, alliesOf(u),
foesOf(u), {})`(`sgz.py` 對應為 `elif t0.get("choices"): apply_effects(u, main_hit_tgt,
t, allies_of(u), foes_of(u), no_heal=False)`)——只在 `t0.choices` 為真(=本次 `t` 是
choices 合成視圖, 非 `u.tactics` 原始物件)時才補套用分支的 `effects`, 刻意**不**對一般
無 `choices` 的 `command`/`passive` 戰法套用(那些戰法的 `effects` 已由 `applyPassives()`
的 `prep`/`healOnly` 通道正確處理, 若無差別補上會造成雙重結算)。

**與 `healOnly` 常駐通道的關係(brief 防雷點已釐清)**: choices 戰法的 `t0.effects` 本身為空
陣列(內容全在 `choices[].effects` 裡), `applyPassives()` 的 `healOnly` 逐回合掃描對空
`effects` 天然 no-op, 不會與此處的一次性套用重複治療。選支內含 `heal` 時的語意採用「選定後
單次觸發, 非窗口內每回合持續」——原文如桃園結義「每回合有20%-40%概率觸發三選一之一」本身就是
逐回合重新擲骰+重新選支(非「選定一次、之後每回合固定用同一分支」), 與 `main loop` 的
`fire=rnd()<t0.rate` 逐回合重新擲骰語意天然一致(每回合都重新走一次 `fire`→`pickChoice`→
套用, 分支不跨回合持久化)。

**已用此能力驗證的機制**: `sgz.py` self-check 78/78b(合成 `command` 型 `choices` 戰法,
單分支巨額 `heal`, 用 `simulate()` 勝率統計驗證分支效果確實套用且未雙重結算)、`engine.js`
同款驗證(node 直接呼叫 `buildPool`+`simulate` 比對套用前後勝率)。桃園結義目前仍維持既有
「固定選治療分支+誠實揭露另兩分支未建模」近似不變(該戰法本身尚未重新設計成 `choices` 版本,
本節僅記錄「若未來要用 `choices` 重建三選一, 引擎端已無阻礙」, 不在本批一併重建——桃園結義
的三分支傷害輸出量級/選標仍需額外資料查證, 非本批 A/B/C 範圍內的既有揭露問題)。

## 18. `heal` 絕對量公式疑似系統性高估(~1.6~2倍) —— 待校準, 本批不動公式

**發現於**: 批24, user 遊戲截圖實測(`docs/data/calibration_anchors.json` →
`heal_firstaid_20260704`, 陷陣營急救效果)。

**觀測**: 夏侯惇(智力379.02, 觸發前兵力約8331)受到急救觸發(治療率60%, `scale:"intel"`),
實測回復量 **546**(user 確認未觸傷兵池上限, 即公式值本身就是546, 非被封頂後的殘值)。

**引擎現行公式重算**(`applyEffects()` heal 分支: `want = hcoef * caster.troop * 0.10 *
boostMult`, `hcoef = coef * SCALE(intel)`, `SCALE(v) = max(0, 1+(v-100)/350)`):
`SCALE(379.02) ≈ 1.797`, `hcoef ≈ 0.6 × 1.797 ≈ 1.078`, `want ≈ 1.078 × 8331 × 0.10
≈ 898`。

**偏差**: 引擎公式值(≈898~1080, 依採用的即時兵力估計略有浮動)相對實測值(546)高估約
**1.6~2倍**，超出 coordinator 訂定的 ±30% 校準容忍帶。

**已排除的干擾因素**: 同一批次的另一實測樣本(智力385, 普攻傷害189, 恢復170)precisely
驗證 `189 × 0.90(第1~3回合傷兵池轉化率) = 170.1 ≈ 170`——傷兵池封頂邏輯(`WOUNDED_RATES`)
本身精確無誤, 偏差不是傷兵池機制造成，而是 heal 絕對量公式(`troop × 0.10 × hcoef` 這條
公式本身的量級)可能系統性偏高。

**本批決策(遵 coordinator 指示)**: 不擅自調整 heal 公式常數(`0.10` 這個絕對量基準或
`SCALE` 曲線), 待統籌者裁決(可能需要更多實測樣本才能反解出精確常數, 目前僅2組樣本、
且只有1組是"未封頂"的乾淨樣本, 樣本量不足以直接反解新常數)。此節僅記錄偏差供後續校準
批次參考, `heal` 相關的 `_note`/`_todo` 揭露維持現狀不變(治療率/scale 語意本身建模正確,
只有最終絕對量可能需要整體乘上一個待定的校正係數)。

---

## 19. `lint_tactics.py` R16-R20 為「類殲滅」防線(批25新增)

**背景**: 連續多批盲測維持在高原(66/71/64/67分), 診斷發現每批只修「被抽中的條目」,
同類病在未抽中的兄弟條目上原地不動(如 v10 抓到丹陽兵/擊其惰歸掛著 stale dmgType 註記,
批24 只修了暫避其鋒等 5 筆代表性條目, 未做全庫同類掃描)。批25 起改變方法論: 每個殘差類
先寫 lint 規則、全庫掃描, 再逐筆清零——「修一個類 = 絕跡一個類」, 而非「修一個代表 =
留下一堆同病兄弟」。

**R16(回合閘門缺失)**: 原文含「第N回合(時)」描述單一時間點爆發, 但戰法 `coef>0` 且無
`t.when` 閘門(`command`/`passive` 每回合都會判定觸發, 造成~N倍放大, N=戰鬥總回合數)。
低誤報設計: 排除「第N回合起/開始」(那是持續窗口語意, 已由 `dur`/R4 天然覆蓋), 只抓
純粹的單點事件; 揭露豁免要求文字提及回合/when/時機等主題關鍵字, 不接受無關主題的舊
揭露頂帳。批25初掃 1 筆(絕地反擊: 「第5回合時...對敵軍全體造成兵刃傷害」但 `rate=1`
每回合必發, 改用 `rate=0.125`(=1/8回合EV折算, 比照士別三日既有慣例)+ `_approx` 誠實
揭露 when 連坐限制(見6.4節), 不強行加 `t.when` 破壞同戰法內 `stat` 疊層效果的常駐語意)。

**R17(反應式治療缺失)**: 原文含「受到傷害時/後...恢復/治療」但 `heal` 效果無
`when.on` (=被建模成常駐每回合治療, 而非受傷當下反應式觸發)。揭露豁免要求文字提及
`when.on`/反應式/onHit, 純舊式 EV 折算說明(`prob-ev`)不算數——這正是本規則要抓的
「還沒升級成精確 when.on 建模」的殘留舊近似。批25初掃 2 筆(青囊、援救), 皆比照
陷陣營/草船借箭的 `e.when.on:"damaged"` + `e.rate` 精確重建慣例修復。

**R18(無效佔位)**: 效果值全為0(`heal.coef==0`/`mitig.val==0`/`amp.val==0`/`stat.add==0`
且`mult==1`)但原文描述實質機制動詞, 且無「文字」揭露(裸 `_est:true` 布林旗標不算——
過去 `has_disclosure()` 誤把裸旗標當成「已誠實揭露」, 實則毫無解釋內容)。與既有 R5
互補: R5 要求原文有明確數值才觸發, R18 專門補「原文完全沒給數字」的盲區(如臨危救主
「我軍普攻有機率恢復兵力最低者並獲謀略減傷」一個數字都沒有)。批25初掃 2 筆(臨危救主
的 heal+mitig 兩效果皆為0), 因無官方數值可查, 改用同類戰法(江東小霸王)量級的保守
估計值 + 完整文字揭露(`_est` 待未來校準)重建, 不再是空轉佔位。

**R19(dmgType適用而未用)**: 原文明說「兵刃/謀略傷害降低/提高」等單一傷害類型措辭
(排除「兵刃傷害和/與/及/、謀略傷害」雙型並列的合法不分型寫法), 但緊鄰數值綁定的
`amp`/`mitig` 效果缺 `dmgType`。低誤報設計比照 R1 的緊鄰數值綁定手法(容忍度<=6%),
避免像初版設計「戰法內任一單型措辭即懷疑全部amp/mitig」的過寬綁定誤傷(如長驅直入
「使自己造成兵刃傷害提升」實際對應 `stack` 效果而非其 `mitig`); 另有會心/奇謀EV折算
特例(`_approx=="crit-ev"`, 原文固定寫「觸發時XX傷害提高100%」但落地值是機率×倍率
折算後的小數, 用 who/身份關鍵字消歧義, 不靠數值比對)。批25初掃 21 筆, 逐筆補
`dmgType`(奇計良謀額外用 `targetSel:"maxForce"/"maxIntel"` 拆成兩條精確表達的獨立
效果, 取代舊有「完全重複合併成一條」的誤判)。

**R20(stale能力聲明總巡檢)**: `_todo`/`_note` 等揭露文字聲稱「引擎無X/不支援X/無對應
原語」, 但 X 實際上是引擎已支援的能力。制度化批24 A項「stale揭露清剿」的手工做法:
把「引擎已支援的能力清單」做成 `ENGINE_CAPABILITY_ALIASES` 資料結構(見
`lint_tactics.py`), 未來新原語落地時只要更新這份清單, 舊 stale 註記就會被自動抓出,
不必每批重新人工全庫翻找。批25初掃 3 筆:
  - 定軍斬將: 頂層 `_todo` 聲稱「引擎無狀態免疫原語」, 但 `immune`(批16新增)早已在
    本戰法落地(`_note` 已正確說明), `_todo` 從未同步清除, 清除後改由 `_note` 承擔
    完整說明。
  - 魚鱗陣: `mitig._note` 聲稱「引擎無單次格擋原語」, 但 `block`(批22新增)早已存在
    ——真正的限制是「持續性機率補充」場景 `block` 本身不適用(見11節), 改寫措辭精確
    指出這一點, 而非籠統宣稱原語不存在。
  - 箕形陣: `mitig._approx` 聲稱「mitig原語不分兵刃/謀略」, 但 `dmgType`(批24新增)
    早已存在——真正的限制是 `who:"subs"` 集合式選標無法讓兩名副將各自套用不同
    `dmgType`(見16節), 改寫措辭澄清。

  能力別名清單目前收錄(`ENGINE_CAPABILITY_ALIASES`): 單次格擋(`block`)、狀態免疫
  (`immune`, 僅涵蓋5種控制類型, 不含healblock等非控制狀態)、傷害類型過濾/區分/
  兵刃謀略(`dmgType`)。刻意保守只收錄「批25核對後確認高信度、易誤稱不支援」的別名,
  避免用寬鬆關鍵字誤傷語意精確、目前真的不支援的合法揭露(如「引擎無per-instance
  機率原語」這類措辭不應命中任何別名)。

**驗收基準(批25版)**: 全庫 R1-R20 應維持零違規常態; 新增戰法或修改既有戰法時, 跑
`python lint_tactics.py --summary` 應為 0 違規才算合格。`reparse_effects.py` 保持
冪等(兩輪 byte-diff 為空)。

## 20. 批26新增: `R21`(點數誤用mult防線) + 效果級 `e.ifLeader`(主將條件閘門) + `stack.stackPer`(每次發動遞增)

**背景**: 盲測v11(76分)的4筆0分+16筆1分病因統計, 三大類病因: (1)R21類點數誤用mult回歸
(全庫恰2筆, 其中1筆是「聲稱已修但只修一半」的stale局面); (2)原文「自身為主將時額外XX」
被折成無條件套用(3筆代表: 南蠻渠魁/江東小霸王/酒池肉林), 缺對應原語; (3)原文「每次發動後
XX提升」的疊加語意(2筆代表: 水淹七軍/陷陣突襲), 現有 `stack` 只支援逐回合遞增, 無法表達
「每次成功發動」這個不同的遞增時機。

**R21(點數誤用mult)**: 見上方6.3節。全庫掃描恰2筆(奮矛英姿/魚鱗陣), 皆已修正為
`stat.add`。

**效果級 `e.ifLeader`(批26新增)**: 施放者須為隊伍主將(index 0, 即 `allies[0] is caster`)
才套用該效果段, 布林旗標, 任何 `k` 皆可掛(與 `e.rate` 同層級, 在 `apply_effects()` 的
`k` 分派判斷式之前統一檢查, 見 `sgz.py`/`engine.js` 對應段落)。全庫「為主將時/主將時」
掃描共40筆(含重複), 篩出未去重戰法約35筆, 其中已有既有揭露(承認此缺口)的約31筆不變動
(未來若有引擎能力可以精確表達可再優化, 本批專注新落地/未揭露的部分); 4筆先前完全未揭露
(坐斷東南/天下無雙/燕人咆哮/神火計), 其中天下無雙可直接用 `ifLeader` 精確落地(已修正),
其餘3筆的主將條件疊加在更複雜的複合機制上(獨立時機判定/頂層coef時間窗擴充/命中後條件式
追加狀態), 現有 `ifLeader` 單一布林閘門不足以表達, 已誠實揭露具體阻塞點(而非籠統寫「未
建模」)。三筆核心落地(南蠻渠魁/江東小霸王/酒池肉林)+1筆額外掃描落地(天下無雙)共4筆完成。

**`stack.stackPer`(批26新增, "round"預設向後相容/"cast")**: `stack` 效果新增可選欄位
`stackPer`, 決定疊層遞增時機: `"round"`(預設, 沿用既有 `fight()` 主迴圈逐回合遞增邏輯,
向後相容全部既有 `stack` 資料)、`"cast"`(不受回合數影響, 改由 `apply_stack_cast()`
在戰法本次成功發動(`fire`)結算處呼叫遞增, 見 `Unit.apply_stack_cast()`/`applyStackCast()`
定義, 與 `fight()` 主迴圈的 `fire` 分支呼叫點)。刻意新增獨立欄位而非覆寫既有 `per`(每層
增傷倍率數值欄位)語意, 避免型別混淆。已落地水淹七軍(「第二次及之後施放，使敵軍受到兵刃
傷害提升」, per=0.1/max=3)、陷陣突襲(依 agy 查證的最新官方文字重建, 「每次額外攻擊觸發後
突擊發動率+3%」, per=0.03/max=6, 工程近似: stack 只影響 `caster.amp()`, 與原文「提高
發動機率」的語意並非嚴格等價, 已誠實記錄此近似落差)。全庫「每次發動/每次施放/每次觸發」
掃描額外抓到同戰身先1筆, 但核對後發現是「發動機率隨發動次數**遞減**」(自我節流, 方向與
`stack` 遞增相反), 現有 `stack.stackPer` 無法表達"讀寫 `t0["rate"]` 本身"這種動態調整
機率的機制, 維持未建模並更正舊 `_note` 的錯誤描述(舊note誤稱速度提升本身是疊加語意,
實際核對原文句構後確認速度提升是固定值, 非疊加, 已修正措辭)。

**驗收基準(批26更新)**: 全庫 R1-R21 應維持零違規常態(取代上方批25版「R1-R20」的舊基準)。

---

## 21. 批27新增: `on:"dealtDamage"`(反應式觸發, 施放者視角) + `choices` 對 `command`/`passive`/`on_hit()` 反應式路徑全面生效 + `R22`(dealtDamage可用而未用防線)

**背景**: v11病因統計最大殘餘槓桿=反應式/條件觸發家族(見上方§8/§18a/第15節)。批27針對其中
兩類「引擎缺口已被前批誠實揭露, 但一直停留在缺口本身、未實際補上原語」的項目落地: (1)
`when.on` 過去只有 `"attacked"`/`"damaged"`(受擊者視角), 沒有「自己造成傷害時」(施放者視角)
的對稱掛鉤; (2) `choices` 只在 `command`/`passive`/`active` 的常駐輪詢派發路徑生效(且批27前
`command`/`passive` 型還有§18a的「讀了但丟棄」bug), `on_hit()` 反應式路徑完全不讀 `choices`
(§8)。

**`on:"dealtDamage"`(批27 A新增)**: 掛在 `hit()` 傷害結算後, 對 `src`(施加本次傷害的一方)
掃描其 `on_deal_tacs`(戰法級, 與 `on_hit_tacs` 對稱)/`on_deal_effect_tacs`(效果級, 與
`on_hit_effect_tacs` 對稱)。只在**非規避**(dodge 分支排除, 攻擊未命中視為未造成傷害;
`block`/`shield` 把傷害吸收/打折歸零不排除, 因為「造成傷害」語意上仍是「打出了這一擊」)且
`src.alive` 時觸發, 同回合節流沿用既有 `hit_flags`(與 `on_hit` 共用同一 set, 用不同
`id(t)`/`id(e)` 鍵區分, 不會互相誤判)。選填 `when.dmgType`(`"phys"`/`"intel"`): 區分
「造成兵刃傷害時」vs「造成謀略傷害時」兩種不同觸發條件(如白衣渡江 disarm 段只認兵刃、
silence 段只認謀略), 未指定視為兩種傷害類型皆可觸發(向後相容)。驗收: `sgz.py` self-check
77(a~f), 涵蓋觸發/dmgType過濾/同回合節流/dodge排除/死亡src排除/預篩陣列正確性; `engine.js`
用合成 `Unit`+`hit()` 直接呼叫驗證同款行為(hook觸發/dmgType過濾/dodge排除三項皆PASS)。

**落地遷移(A)**: 全庫掃描「(自身/自己)?造成(兵刃/謀略)?傷害(時|後)」句型, 排除已由 `lifesteal`
(倒戈, `hit()` 內建按傷害量比例回復機制, 非反應式回合輪詢)精確表達的戰法(據水斷橋/魯莽等)後,
剩 3 筆真正需要 `dealtDamage` 掛鉤: 白衣渡江(disarm/silence 兩效果各自綁 `dmgType:"phys"`/
`"intel"`, 取代批15「反應式觸發機制當時尚無掛鉤點, 簡化為準備階段一次性必定套用」的高估近似)、
深謀遠慮(heal 段補 `when.on:"dealtDamage"+dmgType:"intel"`, 取代舊版「每回合無條件常駐治療,
與頂層coef段是否真的命中脫鉤」的近似)、非攻制勝(heal 段補 `when.on:"dealtDamage"`, 保留原有
`from:3` 回合窗口, 取代舊版「視窗開啟後無條件常駐治療」的近似)。

**`choices` 對 `command`/`passive` 生效(批27 B)**: 見上方§18a, 已解決。

**`choices` 對 `on_hit()` 反應式路徑生效(批27 C)**: 見上方§8, 已解決。魅惑(混亂/計窮/虛弱
三選一)已用 `choices`(三分支均分權重: chaos(n:1)/silence/amp(val:-1, n:1, 虛弱慣例))
精確重建, 取代批20「choices在on_hit路徑形同虛設, 固定選silence一種」的近似。「若自身為女性,
觸發機率額外受智力影響」仍未建模(見上方第9節「性別/陣營等身份條件縮放」缺口, 與本批
choices/dealtDamage是不同機制形狀, 非本批範圍)。

**R22(dealtDamage可用而未用)**: 比照 R17(反應式治療缺失, 受擊者視角)設計同款規則, 但抓的是
「造成傷害時/後」(施放者視角)句型且戰法/效果皆無 `when.on=="dealtDamage"` 且無揭露的情形。
低誤報設計: 逐子句排除「倒戈」關鍵字子句(lifesteal 已由 `hit()` 內建機制精確表達, 非反應式
回合輪詢, 不算本規則管轄); 排除純「受到傷害時」(被打視角, 已有 attacked/damaged 機制)。
`ENGINE_CAPABILITY_ALIASES`(R20) 新增別名「造成傷害時」→`dealtDamage`, 未來若有 `_note`/
`_todo` 仍聲稱「引擎無造成傷害時反應式掛鉤」會被自動抓出。

**D項機會主義小項**: (1) 同戰身先「該戰法每次發動後發動率降低10%」—— 評估 `stack.per`
負值可行性, 結論**不可行**: `stack` 只影響 `Unit.amp()`(單位自身的增傷倍率), 與 `rate`
擲骰判定(讀取 `t0["rate"]`, 一個跨所有持有該戰法單位共用的靜態 dict 值)是完全獨立的兩個
資料通道, 負值 `per` 只會讓傷害輸出隨層數遞減, 不會影響戰法本身的觸發機率, 兩者是不同維度
(效果數值 vs 觸發機率)、不同資料生命週期(per-unit狀態 vs 跨unit共享的靜態戰法定義)的
機制, `stack` 原語無法迂迴表達"讀寫戰法自身rate"這件事, 維持既有 `_todo` 誠實揭露不變, 未
新增資料變動。(2) 千里馳援/刀出如霆(v11「原文模糊需判斷」)——用 grok + agy(Gemini) 雙來源
交叉查證現行官方文字: 千里馳援現有資料(`redirect` share=1.0 + `stat.add` 40統率)與查證結果
一致, 無需改動; 刀出如霆確認 2025年7月官方已將機制從「300%傷害由敵軍全體平分」改版為「180%
傷害重複3次, 可集中打同一目標」, 現有 `coef:1.8`+`hitsRepeat:true` 已正確對應新版數值,
但頂層 `_real`/`_src`/`_conf` 三個欄位仍是改版前的舊推導殘留(描述舊版「300%平分」邏輯,
與現行 `coef` 恰好同量級而不易察覺矛盾), 已清除(見 `tactic_corrections.json` 的
`"刀出如霆"` correction `set` 內三個欄位設為 `null`)並記錄此類「數值已更新但推導敘述未同步」
的 stale 態樣供未來參考。

**驗收基準(批27更新)**: 全庫 R1-R22 應維持零違規常態(取代上方批26版「R1-R21」的舊基準)。

---

## 22. 批28新增(補記): `counter.guardFor`(守護式反擊) + `amp`/`mitig`/`redirect` 的 `normalOnly`

**背景補記**: 批28上線 `counter.guardFor:"leader"` 與 `amp`/`mitig`/`redirect` 的
`normalOnly` 兩個新原語, 但當時未同步更新本文件附錄的原語清單, 也未同步 `lint_tactics.py`
R20 的 `ENGINE_CAPABILITY_ALIASES`——導致批26-28三批新增的原語(`ifLeader`/`stackPer`/
`dealtDamage`/`guardFor`/`normalOnly`/`choices`傷害段擴充)全部沒有登記進R20別名表,
江東猛虎的 `mitig` 效果從批10留到批29都還在用「引擎無條件判斷」「引擎mitig無傷害來源
區分」這種批26/28後已stale的措辭, 卻沒有任何機制自動抓出。批29 A 已把此文件的滯後
也一併補上(見下方)。

**`counter.guardFor:"leader"`**: `k:"counter"` 效果新增選填欄位 `guardFor`, 值為
`"leader"` 時, 該效果不掛在持有者自己身上, 改登記進隊伍主將(`allies[0]`)的
`counterGuards` 清單, 由 `hit()` 在主將受到**普通攻擊**(`isNormal===true`, 戰法傷害
不觸發)時, 由登記者代為對攻擊者反擊(每個守護單位每回合最多觸發1次)。用於表達「A(如
隊伍主將)受擊時, B(如副將/自身)代為反擊」這種方向與持有者自己反擊(`counter` 不帶
`guardFor`)相反的守護語意(見虎衛軍/古之惡來)。限制: 只支援 `guardFor:"leader"`,
無法表達「守護指定的其他副將」等更細緻的守護目標。

**`normalOnly`(`amp`/`mitig`/`redirect` 皆支援)**: 選填布林旗標, 限定該效果只在
**普通攻擊**造成/受到的傷害上生效/計入, 戰法/突擊等非普攻傷害不受影響(`amp`/`mitig`
見 `addbonus()` 的 `isNormal` 參數過濾; `redirect` 見 `Unit.guardNormalOnly`, `hit()`
判斷 `dst.guardNormalOnly && !isNormal` 時不轉移傷害)。用於表達原文「受到普通攻擊
傷害降低」這類限定攻擊型態的措辭(見江東猛虎「自身為主將時，使自身受到普通攻擊傷害
降低」)。

**批29 A: R20 漂移偵測制度化(制度性堵洞)**——手工維護 `ENGINE_CAPABILITY_ALIASES`
每次都可能忘記登記新原語(批26-28三批全部忘記), `lint_tactics.py` 新增
`_scan_engine_js_tokens()`(自動盤點 `docs/engine.js` 原始碼裡的 `k===` 分派值、
`e.xxx` 效果欄位讀取、`when.on`/`guardFor`/`stackPer` 字面值分派)+
`check_r20_capability_drift()`(與 `ENGINE_CAPABILITY_ALIASES` 交叉比對, 有實作但
無別名描述提及的 token, 排除 `CAPABILITY_INVENTORY_IGNORE` 白名單後視為「新原語落地
忘記登記」的缺口), 已納入 `--selftest` 的失敗條件(非僅警告)。同時修復 R20 判定本身
的方向性偏見: 原本只查「否定詞在能力關鍵字前方」的窗口(如「引擎無『X』原語」), 漏掉
「能力關鍵字在前、否定詞在後」的句型(如「限自身為主將時觸發,引擎無條件判斷」), 改為
前後皆查, 並用「否定詞需緊鄰(<=6字)且中間不含引號」排除「否定詞其實指向另一個引號內
無關名詞」的假陽性(見批29實測案例: 挫銳/錦囊妙計)。

**驗收**: 補齊 `ENGINE_CAPABILITY_ALIASES` 後全庫 R20 重掃現形 1 筆(江東猛虎), 已於
批29 B 重建清零。

## 23. 批29修復: 古之惡來/虎衛軍 頂層coef殘留與guardFor並存的雙重計算

**發現**: 批28已把古之惡來/虎衛軍的反擊機制遷移到 `counter.guardFor:"leader"`, 但兩筆
的頂層 `coef`/`rate`/`n` 從未被 `set` 清零——`command` 型戰法的常駐派發路徑
(`t0.coef` truthy 時每回合 `rnd()<t0.rate` 擲骰, 命中則對 `n` 個隨機敵軍發動無條件
攻擊)與新遷移的 `guardFor` 守護反擊**並存**, 造成雙重計算(每回合仍會額外對隨機敵軍
發動一次無條件群攻/單體攻擊, 與原文「主將受普攻時才觸發」的條件語意完全不符)。
此類「頂層殘留欄位與effects層級新機制並存」的bug**不會被任何現有lint規則抓到**——
R6(雙重計算)只檢查 `effects` 陣列內是否同時有 `counter`/`settle`, 不檢查頂層 `coef`
是否該歸零而未歸零; 這是本次盲測病因排查後才發現的制度性死角, 非本批新增規則涵蓋範圍
(供未來規則設計參考: 若某戰法的核心機制已完全遷移到反應式/守護式效果, 頂層 `coef`
理論上應歸零, 但目前沒有「reactive-only 戰法不該有頂層 coef」的自動化檢查)。

**修復**: 兩筆頂層 `coef:0`(古之惡來 `n:1`維持不變、虎衛軍 `n:3→1`), 關閉舊派發路徑。
古之惡來同時把 `redirect`(全體代承, 過度近似)改為 `counter.guardFor:"leader"`
(精確表達「主將受普攻時, 自身代為反擊」), 「代為承受此次普攻傷害」本身(而非反擊)的
轉移語意仍未建模(見 `tactic_corrections.json` 該筆 `_todo`, `redirect` 機制無法表達
「僅主將+僅此次」的精確傷害轉移, 屬既有能力邊界)。

**驗收**: `sgz.py` 直接呼叫 `apply_effects`/`hit` 驗證 guardFor 反擊正確觸發於主將
受普攻時、`ifLeader`+`normalOnly` 正確閘門江東猛虎的 mitig 效果, 見批29驗收腳本。

## 24. 批29 C: `e.when.parity` 已可精確表達奇偶回合交替(垂心萬物), `extraHits`+`ifTargetHas`
已可精確表達「若目標已處於某狀態則造成傷害」條件分支(神機莫測)——兩者皆非結構性缺口,
只是先前未落地套用既有原語

**垂心萬物**: 原文「奇數回合35%→70%機率(增傷/攻擊分支)...偶數回合35%→70%機率
(治療分支)」過去用頂層 `coef`+共用 `rate` 折半近似(把兩個獨立的奇偶回合機率揉在一起,
借用同一個 EV 折算值)。`e.when.parity`(批16已支援, 見第16節後的原語清單/武鋒陣既有
precedent)其實已可精確表達: 頂層 `t.when:{parity:"odd"}` 閘門 coef 段(僅奇數回合
擲骰), heal 效果自帶 `e.when:{parity:"even"}` 閘門治療段(`heal` 的 `opt.healOnly`
路徑本就會讀 `hw=e.when||t.when` 並用 `roundOk` 判斷, 支援 `parity`), 兩分支各自
用共用的 `t.rate`(兩分支機率範圍恰好相同, 取滿級值)獨立擲骰, 不再需要 EV 折半近似。

**神機莫測**: 原文「若目標已混亂(敵軍), 對其造成謀略傷害(傷害率175%)」條件分支, 批26
評估認為需要 `extraHits`+`ifTargetHas` 搭配、且超出當批授權範圍而擱置。批29 C 確認
`extraHits`(批13已支援) 的 `ifTargetHas`(批16已支援) 欄位其實已完整具備所需能力:
`fireExtraHits()` 在 `applyEffects()`(主 coef/chaos 效果套用)**之前**執行(見
`engine.js`/`sgz.py` 呼叫順序), 故 `extraHits[].ifTargetHas:"chaos"` 檢查到的必然是
「進入本回合前已持續存在」的混亂狀態, 不會誤判本回合稍後才施加的新混亂, 精確對應原文
「已混亂」(既有狀態)語意, 不需要任何新原語。落地後全庫 R1-R22 仍維持零違規。

**啟示**: 這類「評估時判斷需要新原語, 但實際上現有原語組合(順序特性/欄位交叉使用)已
足夠」的情形, 建議未來評估「是否為結構性缺口」時, 先窮舉現有原語的**執行順序**與
**欄位可組合性**, 而非只看單一欄位是否存在。

## 25. 批29 D: 6筆v13真結構缺口的最小設計評估(未實作, 供批30決策)

逐筆讀原文後歸納, 6筆缺口可分為 4 類最小設計需求(部分共用同一類缺口, 見下表), 影響
筆數為粗估(全庫掃描同類措辭可能還有更多戰法受益, 未逐一清點, 見 D 項報告本體)。

1. **狀態鏡射/廣播事件**(機鑑先識「隊友中招時, 反向施加同狀態給敵方」): 需要在
   `stun`/`silence`/`disarm`/`chaos` 實際套用到某單位的那一刻(`applyEffects` 的
   CTRL_K 分派段), 廣播一個新事件供**另一個單位**(非受影響者本人)的反應式效果監聽,
   與第15節「`allyAttacked` 廣播」是同一類「事件廣播機制」缺口的不同觸發點版本
   (第15節是「受擊」事件, 這裡是「狀態套用」事件), 建議合併評估、一次性擴充事件
   廣播基礎設施(而非各打各的補丁)。
2. ~~**非heal效果的逐回合機率重擲**(機鑑先識「每回合21%→42%機率再獲得1次警戒」)~~
   —— **批30 A 已解決**: 新增效果級 `e.everyRound` 通用逐回合重擲通道, 見第26節。
3. ~~**依隊伍位置分派相異效果細節**(箕形陣「副將A只防兵刃, 副將B只防謀略」)~~ ——
   **批30 C 已解決**: 新增 `who:"sub1"`/`"sub2"` 固定位置選標, 見第26節。
4. **動態資源池**(丹陽兵「依上回合輸出動態補充, 每回合衰減10%, 消耗以抵擋傷害」)與
   **命中計數觸發**(捨身救主「每次受擊-1.5~3%, 上限30次; 降至5次後X%機率視為2次
   傷害」): 兩者都需要「非時間驅動、而是事件次數驅動」的狀態量, 且丹陽兵的補充源
   還額外依賴另一個獨立指標(上回合己方輸出總傷害), 是全庫目前最複雜的資源模型
   缺口, 建議獨立評估、不與其他3類混在同一個「狀態量」擴充裡(丹陽兵、捨身救主兩者
   彼此的資料流向也不同, 不應合併成單一原語)。

**額外發現(非6筆缺口本身, 但評估過程中發現)**: 濟貧好施的 `mitig` 段(26%減傷限
第3~5回合、僅兵力最低單體)目前仍用 EV 折算(`_approx:"prob-ev"`, 時間佔比3/8≈0.10
常駐), 但其實可仿照同戰法 `heal` 段已用的 `e.when:{from:3,until:5}` 精確表達(見
批29 C 驗證的同一批「原以為需要新原語, 實際只是沒套用既有原語」教訓), **不屬於
D項6筆「真結構缺口」之一**——**批30 B 已完成此升級**, 見第26節。

---

## 26. 批30: 常駐通道泛化 —— `e.everyRound`(非heal效果逐回合重擲) + `who:"sub1"/"sub2"`(副將固定位置) + 濟貧好施 mitig 精確化

**背景**: 批29 D 評估的4類v13真結構缺口中, 第2類(非heal效果逐回合重擲)與第3類(依隊伍
位置分派相異效果)在批30落地; 另順手完成批29 D 記錄的「濟貧好施 mitig 段可仿照 heal
段用 `e.when` 精確表達」待辦。

### A. `e.everyRound`(效果級旗標, opt-in)

**設計**: `apply_passives(heal_only=True)`(`engine.js` 對應 `applyPassives({healOnly:true})`)
過去只有 `k=="heal"` 會被逐回合重新掃描/套用(常駐治療通道), 其餘 `k`(`amp`/`mitig`/
`block`/`stat`/`insight`/`redirect`/... )一旦在 prep 階段(`apply_passives(no_heal=True,
skip_when_effects=True)`)套用一次後, 不會再被重新判定, 導致「每回合X%機率獲得/觸發
某效果」類戰法只能 EV 折算成穩態常駐值, 或截斷成一次性授予(見第11節/批29 D 第2類)。

`apply_effects()`(`sgz.py`)/`applyEffects()`(`engine.js`) 新增判斷: 效果帶 `e.everyRound
:true` 時, 只在 `heal_only`/`opt.healOnly` 呼叫路徑套用(其餘路徑, 包含 prep, 一律跳過
——對稱於 `heal` 只在 `heal_only` 路徑走完整判定的既有慣例), 套用前的閘門邏輯與 `heal`
完全一致(泛化自 `heal` 分支既有實作, 抽出共用邏輯):
1. `e.when`(效果級優先)或 `t.when`(戰法級)存在 → 用 `round_ok` 檢查回合是否落在視窗內;
   `when.rounds`(明確列出的特定回合)用 `heal_rounds_fired`(與 heal 共用同一份 dedup 狀態,
   鍵為效果物件本身, 不會與 heal 撞鍵)去重「每個列出回合各觸發一次」; `when.from/until`
   (範圍視窗)不去重, 窗內每回合都重新判定。
2. 無 `when` 但 `e.once` → 用 `when_fired` 去重(觸發過一次即不再結算)。
3. 皆無 → 每回合都重新判定(不去重)。
4. 以上通過後擲 `e.rate ?? t.rate ?? 1` 骰, 未中則本回合不套用(不消耗任何去重狀態,
   下回合可再擲)。

判定通過後**不** `continue`, 而是落到函式下方通用的 `who`/`dests` 選標與 `k` 分派邏輯
(`amp`/`mitig`/`block`/`stat`/`insight`/`redirect`/... 皆可掛, 與 prep 套用走同一套
分派程式碼, 只是改為每回合可能重新觸發一次)。`push_add`/`push_mod`/`push_stat_add`
(同源刷新覆蓋)與 `push_block`(同源疊次)的既有語意不受影響——重複套用同一效果只會
刷新或依 `block` 自身疊次規則疊加, 不會產生非預期的無限增長。

**驗收**: `sgz.py` self-check 84(合成 `block` 效果帶 `everyRound`, 驗證: prep 路徑不
套用/heal_only 路徑逐回合重新擲骰疊次/rate=0時不套用/對照組不帶 everyRound 的效果
行為零回歸)。`engine.js` 用合成 `Unit`+`applyEffects`+`trace()` 直接呼叫驗證同款行為
(prep 不套用/多回合戰鬥中 `trace()` 日誌可見「獲得...抵禦」重複觸發多次)。

**遷移(9筆)**: 機鑑先識(準備階段固定2次警戒不變, 新增「每回合21%→42%機率再獲得1次
警戒」段, `n:2~nMax:3, rate:0.42, when:{until:3}`)、揮兵謀勝(「前3回合每回合35%→70%
機率獲得1次抵禦」, `block val:1.0 + everyRound + rate:0.7 + when:{until:3}`)、魚鱗陣
(同揮兵謀勝結構, `rate:0.25`)、枕戈坐甲(兵書, `docs/data/bingshu_parsed.json` 直接
編輯, 該檔無 reparse pipeline, 手動維護, `rate:0.25, when:{from:2,until:6}`)、
剛柔並濟(`mitig`/`amp` 兩段, `rate:0.6`, `mitig` 段補 `targetSel:"minTroop"` 精確選中
我軍兵力最低單體; `amp` 段因引擎無 `maxTroop` 準則, 維持 `who:"enemy"` 全體近似, 已
誠實揭露不可誤用 `minTroop` 替代, 見第17節)、忠勇義烈(5個效果各自獨立 `rate:0.6`,
對應原文「每種效果獨立判定」)、南蠻渠魁(`amp` 段補 `everyRound`, 與既有 `ifLeader`
並存, 取代批26遺留的「開局擲一次骰」近似)、承天靖世(`silence` 段, `rate:0.12`)、
鎮扼防拒(`redirect`/`heal` 兩段, `redirect` 補 `everyRound+rate:0.5`; `heal` 段本就有
`heal_only` 常駐通道, 不需要 `everyRound` 旗標, 改用既有 `e.rate:0.5` 機制取代 EV
折算)、虛實奇謀(2個 `insight` 段, `rate:0.5`/`0.35`)、雁行陣(偷統率組合表達的2條
`stat` 效果, 補 `everyRound`, 取代「prep單次擲骰」近似)、高櫓連營(`amp`/`disarm` 兩段,
`rate:0.5`/`0.75`)。

**未遷移(誠實揭露, 非本批範圍)**: 蓄勢待發(`dodge` 機率無可查證數值, 已是 `_est` 佔位,
不強行套用 `everyRound` 增加另一層未驗證假設)、溯江搖櫓(`stun`/`dispel` 為「自身造成
傷害時」反應式觸發, 非literal「每回合」, 需要 `when.on:"dealtDamage"` 掛鉤〔批27已
支援〕而非 `everyRound`, 屬不同機制形狀, 不在本批範圍)、盛氣凌敵/百計多謀/眾志成城/
江東小霸王(核對後確認已是正確既有機制——盛氣凌敵的 `t.when` 視窗+`rate:1` 無重擲需求;
百計多謀/眾志成城為 `active`/heal 型, 已走既有正確通道; 江東小霸王的 `ifLeader` 段是
固定2回合視窗, 非機率重擲缺口)。

**R20 能力別名登記**: `lint_tactics.py` `ENGINE_CAPABILITY_ALIASES` 新增「每回合機率」
「逐回合重擲」「逐回合重新判定」→`everyRound`, 供未來 stale 揭露自動偵測。`KNOWN_EFFECT_
FIELDS` 新增 `everyRound`(跨 k 通用欄位, 同 `rate`/`ifLeader` 慣例)。

### B. 濟貧好施 `mitig` 精確化

`mitig` 段(26%減傷, 限第3~5回合、僅兵力最低單體)改用 `e.when:{from:3,until:5}`(比照
同戰法 `heal` 段既有寫法, `mitig` 效果級 `e.when` 早由批18泛化支援, 只是先前未套用到
本戰法), 取代批15遺留的 EV 折算(`val:0.1`, 時間佔比3/8常駐)。`val` 改回原文滿級值
0.26。`who:"ally"`(全體)vs 原文「僅兵力最低單體」的高估仍未解決(`mitig` 無等價 `heal`
的「最殘一人」動態選標可用), 已誠實揭露維持既有近似。

### C. `who:"sub1"/"sub2"`(副將固定位置選標)

`apply_effects()`/`applyEffects()` 的 `who` 分派新增 `"sub1"`(=`allies[1]`, 副將A)/
`"sub2"`(=`allies[2]`, 副將B), 對稱於既有 `who:"leader"`(=`allies[0]`)慣例, 三人隊
固定編制(index 0=主將/1/2=副將), 隊伍不足對應人數或該位置陣亡則目標為空陣列(不報錯/
不誤選)。與 `who:"subs"`(集合式, 兩名副將套用同一份效果)並存不衝突, 用於表達「副將A
只受X, 副將B只受Y」這種依固定位置分派相異效果細節的語意, `subs` 仍是套用「相同效果給
全部副將」的正確選擇, 兩者語意互補而非取代。

**驗收**: `sgz.py` self-check 85(三人隊, `who:"sub1"/"sub2"` 各帶不同 `dmgType`,
驗證精確命中對應位置、不誤中主將、隊伍不足3人時 `sub2` 為空陣列不誤選)。`engine.js`
用合成 `Unit`+`applyEffects` 直接呼叫驗證同款行為(sub1/sub2 各自的 `dmgType` 正確
寫入 `adds`, 主將無 `mitig` add)。

**遷移(1筆)**: 箕形陣的 `mitig` 段(批25遺留的 `who:"subs"` 合併近似, 兩名副將皆獲得
同一份涵蓋兵刃+謀略雙類型的20%減傷)改用 `who:"sub1"`(`dmgType:"phys"`)/`who:"sub2"`
(`dmgType:"intel"`)精確表達「副將A只防兵刃, 副將B只防謀略」。「隨機」哪一名副將防
哪種類型未建模(`sub1`/`sub2` 是固定位置, 非隨機分派), 已知近似, 但核心「雙方各自只防
單一類型」語意已精確表達, 優於此前「兩人都防兩種類型」的範圍過寬。

**全庫掃描**: 除箕形陣外, 全庫另 8 筆 `who:"subs"` 戰法(三勢陣/夢中弒臣/武鋒陣/潛龍陣/
虎衛軍/鋒矢陣/青州兵)核對後確認皆是「相同效果套用給全體副將」的正確語意(非「副將A/B
各自不同效果」), 不需遷移; 三勢陣的「損失兵力較多的副將受傷-30%, 另一名增傷+25%」是
依**動態兵力比較**決定角色(非固定位置), `sub1`/`sub2` 不適用(需要新的 `targetSel` 準則
在兩名副將子集合內比較, 是不同於本批 `sub1`/`sub2` 的另一個缺口, 維持既有 `split-ev`
近似不變)。

**驗收基準(批30更新)**: 全庫 R1-R22 應維持零違規常態; `reparse_effects.py` 兩輪
byte-diff 為空(冪等); `sgz.py` self-check 全數通過(含新增84/85)。

---

## 27. 批31: `when.on:"activeFired"`(自身主動/突擊戰法發動事件) + `e.activeOnly` + `extraHits.ifSameTargetIsLeader`

**背景**: 盲測v14(78分)唯一0分(士爭先赴)+4筆「條件觸發簡化為無條件」1分, 同屬一族——
效果須綁定「自身主動戰法發動」這個事件本身, 現行卻各自獨立擲骰, 與是否真的有主動戰法
成功發動完全脫鉤。

### A. `when.on:"activeFired"`(反應式觸發, 施放者「自己成功發動主動/突擊戰法」視角)

**設計**: 與批27 `dealtDamage`(自己造成傷害視角)/既有 `on_hit`(自己受擊視角)完全對稱的
第三種反應式事件, 對比前兩者是「傷害結算」相關事件, `activeFired` 純粹是「自身某個
`type:"active"`(或 `type:"charge"`)戰法本次擲骰命中 `fire===true`」這件事本身, 不要求
真的造成傷害(該戰法可能 `coef` 為0純buff)。

`Unit` 新增預篩陣列 `active_fired_tacs`(戰法級, 整個戰法 `t.type` 為 `passive`/`command`
且 `t.when.on=="activeFired"`)/`active_fired_effect_tacs`(效果級, 母戰法無 `t.when`、但
至少一個效果帶 `e.when.on=="activeFired"`), 與 `on_deal_tacs`/`on_deal_effect_tacs` 完全
對稱(同一套設計模式, 只是掃描的 `on` 字面值不同)。`fight()` 主迴圈新增 `active_fired(u)`
函式(與 `dealt_damage()`/`on_hit()` 同一層級的內部閉包), 在 `type:"active"` 戰法
`fire===true` 判定通過(`apply_stack_cast()` 之後、實際套用觸發戰法本身效果**之前**)、
以及 `type:"charge"` 戰法擲骰命中(`hit()`/`applyEffects()` 之後)兩個既有發動點各呼叫一次
`active_fired(u)`。`command`/`passive` 型戰法的常駐每回合擲骰**不**觸發此事件(那是「每回合
固定擲骰」而非「發動主動戰法」, 語意不同)。

`when.timing`(選填, `"before"`/`"after"`): 士爭先赴原文「成功發動...**前**」, 但引擎在
同一回合內對「前/後」無實質結算順序差異(觸發本體戰法與本反應式效果都在同一次 `fire`
判定之後才有意義, 沒有跨回合的「發動前」窗口可插入)——統一在 `fire===true` 判定通過、
實際套用觸發戰法效果之前呼叫 `active_fired()` 廣播(貼近 `before` 語意), `after` 措辭的
戰法一律視同無差別, 不細分兩種處理路徑, 只記錄於資料 `_note` 供文件語意保留。

同回合節流沿用既有 `hit_flags`(與 `on_hit`/`dealt_damage` 共用同一個 set, 不同方向的
觸發各自用不同 `id(t)`/`id(e)` 鍵, 不會互相誤判)。

**方向限制(重要)**: `activeFired` 只覆蓋「**自身**成功發動主動/突擊戰法」這一種方向——
掃描與觸發對象都是同一個 `Unit`(自己監聽自己的發動事件)。原文常見的另外兩種方向:
「**敵軍**發動主動戰法時」(神機妙算/舌戰群儒)、「**我軍全體(含隊友)**發動主動/突擊戰法
時」(經天緯地/十二奇策「下次發動」)皆是跨單位廣播事件(監聽別人的戰法發動, 而非自己的),
`activeFired` 完全無法表達, 仍是未解決的缺口(需要「跨單位事件監聽」基礎設施, 屬於與
第15節「`allyAttacked` 廣播」/批29 D 第1類「狀態鏡射/廣播事件」同一大類但觸發點不同的
子情形, 建議未來合併評估, 一次性擴充事件廣播基礎設施而非各打各的補丁)。

**落地遷移(1筆確認同族)**: 士爭先赴——頂層改用 `t.when:{on:"activeFired", timing:"before"}`
(取代舊版常駐 `coef:1.2`+`rate:0.5` 與「是否真的有主動戰法成功發動」完全脫鉤的近似,
v14盲測0分主因), 仍是同一組 `rate:0.5` 擲骰+`coef:1.2`(120%)對敵軍2人兵刃傷害, 只是
觸發時機點改成「自身某個 `type:"active"` 戰法成功 `fire` 時」才判定, 而非每回合獨立判定。

**評估但排除(非同族, 已確認並修正措辭)**: 將門虎女/萬軍奪帥/暴斂四方/驍健神行(brief
點名的4筆v14「條件觸發簡化為無條件」1分戰法)逐筆讀原文核對後確認**皆非** `activeFired`
家族——四者皆是 `type:"active"` 本身, 內部的條件鏈(虎嗔DoT期間受3次傷害才結算震懾/
目標已處於震懾狀態才禁療/目標已被繳械才觸發額外傷害)是**同一次戰法效果內部的先後順序
依賴**(如「造成傷害的同時附加DoT, DoT期間再受擊3次才觸發強控」), 不是「監聽自身另一次
獨立的主動戰法發動」這種跨事件耦合, 與士爭先赴的「一個獨立被動戰法綁定另一個主動戰法
是否成功發動」形狀不同, 沿用既有 `_note`/`_todo` 揭露(維持既有近似)不變。

**全庫掃描候選(9筆, 逐筆判定)**:
| 戰法 | 方向 | 結論 |
|---|---|---|
| 士爭先赴 | 自身 | 同族, 已用 `activeFired` 精確重建 |
| 鷹視狼顧 | 自身(前4回合) | 同族但受6.4節「母戰法有`t.when`」限制阻塞(見下方), 維持近似 |
| 十二奇策 | 我軍全體「下次發動」 | 非同族(跨單位廣播), 已更正措辭誠實揭露 |
| 陷陣突襲 | 自身(突擊戰法本體) | 同族但受「`type:"charge"`效果不在prep套用」限制阻塞(見下方), 維持近似 |
| 神機妙算 | 敵軍 | 非同族(跨單位廣播), 已更正措辭誠實揭露 |
| 舌戰群儒 | 敵軍 | 非同族(跨單位廣播), 既有措辭已準確, 不變 |
| 經天緯地 | 我軍全體 | 非同族(跨單位廣播), 已更正措辭誠實揭露 |
| 白衣渡江/威震華夏/出其不意/誓守無降/坐斷東南/裸衣血戰/水淹七軍/震駭四境 | — | 假陽性(措辭描述「無法發動主動戰法」等控制效果本身, 非事件觸發條件)或已由其他機制(dealtDamage/stackPer)正確表達 |
| 十勝十敗 | 我軍主將(隊伍固定位置) | 全庫掃描額外發現(非brief點名), 監聽對象是「隊伍index 0」而非「自己」, 一般情形(持有者非主將)仍需跨單位廣播, 已補 `_todo` 誠實揭露, heal段維持缺失 |

**同族但受既有限制阻塞、無法直接遷移的2筆(記錄供未來擴充參考)**:
- **鷹視狼顧**: `lifesteal`(前4回合自身發動主動戰法時100%全軍攻心) 若改掛
  `e.when.on:"activeFired"`, 因母戰法本身已有 `t.when:{from:5}`(供主 `coef` 傷害段使用),
  依第6.4節「母戰法無 `t.when` 限制」, 效果級 `activeFired` 掛鉤(沿用批27 `dealtDamage`
  同款設計)要求母戰法無 `t.when` 才會被預篩收錄——本戰法不滿足此前提, 若強行套用該效果
  會完全不觸發(比現況更糟)。維持既有近似不變。
- **陷陣突襲**: 本體(`chargeup`+`stack` 為常駐自我buff, 傷害段 `coef:0.95` 應綁定「自身
  成功發動突擊戰法」) 若改 `type:"charge"` 讓傷害段走 charge 原生擲骰通道(理論上比掛
  `activeFired` 更輕量, 因為 charge 本身的成功發動就是這個事件), 會導致 `chargeup`/`stack`
  兩個效果從「開戰即生效的常駐buff」退化成「必須先成功發動過一次charge才會套用」(因為
  `apply_passives()` 的 prep 套用迴圈只對 `t.type in ("passive","command")` 套用該戰法
  effects, `type:"charge"` 的 effects 只在 `fire===true` 那一刻套用)——這是比現有「每回合
  無條件95%傷害」近似更嚴重的迴歸, 已評估後撤回, 維持 `type:"passive"` 現況不變。真正的
  修法需要「一份JSON戰法在Unit建構時展開成主體(passive, 常駐buff)+ 附掛的偽charge子戰法
  (傷害段, 走charge擲骰通道)」雙態機制(類似裝備 `proc` 的一次性硬編碼實作, 但需要通用化
  成戰法JSON層可調用), 超出本批範圍。

### B. `e.activeOnly`(amp 效果欄位, 對稱於既有 `normalOnly`)

**設計**: `amp` 效果新增選填欄位 `e.activeOnly`, 限定該增傷只在**主動/突擊戰法造成的
傷害**上生效, 一般普通攻擊傷害不受影響(與批28 `normalOnly` 方向相反的對稱概念:
`normalOnly` 限定「僅普攻」, `activeOnly` 限定「僅主動/突擊」)。實作: `hit()`/`damage()`/
`Unit.amp()`/`Unit.addbonus()` 新增尾端可選參數 `is_active`(Python)/`isActive`(JS,
與既有 `is_normal`/`isNormal` 對稱), 只在明確為 `True` 時才計入宣告 `activeOnly` 的
加成項(呼叫端未傳入時預設 `None`/`undefined`, 安全側不套用, 向後相容全部既有呼叫點
不受影響)。`type:"active"` 主迴圈與 `type:"charge"` 迴圈的傷害呼叫點皆已補上
`is_active=True`(或 `t0.type=="active"` 時為 `True`, 否則 `None`), `activeFired()`
反應式觸發的傷害段同樣標記 `is_active=True`。

**落地**: 士爭先赴的 `amp`(val:0.2, 原文「傷害率60%→120%」的10%→20%增傷)補
`activeOnly:true`, 取代原本無條件套用到自身全部傷害(含普通攻擊)的近似, 精確限定僅對
主動/突擊戰法造成的傷害生效。

### C. `extraHits.ifSameTargetIsLeader`(事後過濾: 主段隨機目標是否恰為敵軍主將)

**發現於**: 批31 A 遷移掃描過程中核對暗藏玄機, 原文「若目標(普攻對象)為敵軍主將,額外
謀略傷害92%」是條件分支(目標是否恰為敵軍主將), 過去因無法直接判斷「主 `coef` 段隨機
選定的 `sameTarget` 是否為主將」, 用 1/3(隊伍3人之一為主將)EV 折算近似觸發率
(`rate:0.34`)。

**設計**: `extraHits[]` 新增選填欄位 `ifSameTargetIsLeader`(布林), 在 `dests` 解析完成
(含既有 `who`/`targetSel`/`ifTargetHas` 過濾)後, 額外過濾只保留 `dests` 中恰好等於
`foes[0]`(敵方隊伍固定主將位)的目標。與批16 `ifTargetHas`(檢查「目標身上是否已有某個
狀態」)是不同概念但同一層級的條件過濾器: `ifTargetHas` 檢查動態狀態, `ifSameTargetIsLeader`
檢查隊伍固定位置身份, 概念上更接近既有 `who:"enemyLeader"` 的固定位置判斷, 但用於「事後
過濾已選定的隨機目標」而非「主動選定目標」, 是不同的判斷時機(前者發生在主段目標已經隨機
選定之後, 後者是 extraHits 段自己主動指定選標準則)。

**落地(1筆確認候選)**: 暗藏玄機——`extraHits[0]` 改用 `who:"sameTarget"`+
`ifSameTargetIsLeader:true`(移除 `rate:0.34` EV折算欄位), 精確表達「若目標恰為敵軍主將,
必定觸發此額外段」的確定性條件分支, 取代舊有機率折算近似。全庫掃描確認除暗藏玄機外,
目前無其他戰法同時具備「隨機選定的sameTarget目標身份判斷」措辭(垂心萬物的同類舊近似
已於批29 C確認為錯誤版本文字並移除, 非本批範圍內的活躍候選)。

**驗收**: `sgz.py` self-check 86(a~c, 涵蓋士爭先赴真實資料端到端驗證: 無主動戰法成功
發動時不觸發/rate=0.5統計上約半數觸發/activeOnly amp判定/hit_flags同回合節流)、87
(ifSameTargetIsLeader 精確命中主將位/不誤中副將位)。`engine.js` 用合成 `Unit`+
`applyEffects`/`fireExtraHits`/`hit`/`damage` 直接呼叫驗證同款行為(activeFired 預篩
陣列正確收錄/rate統計/activeOnly amp過濾/hit_flags節流/ifSameTargetIsLeader過濾五項
皆PASS)。

**R20 能力別名登記**: `lint_tactics.py` `ENGINE_CAPABILITY_ALIASES` 新增「自帶主動戰法」
「成功發動」→`activeFired`(刻意不用方向中性的「發動主動戰法」當別名鍵, 避免把神機妙算/
經天緯地等貨真價實的跨單位廣播缺口誤判成 stale 揭露)、「僅主動戰法傷害」→`activeOnly`、
「目標為敵軍主將」→`ifSameTargetIsLeader`。`KNOWN_EFFECT_FIELDS`/`PER_KIND_FIELDS`
新增 `amp` 的 `activeOnly`(供 R9 幽靈欄位檢查辨識為已知欄位)。

### D. 批31修復: `onHitTacs`/`onHitEffectTacs` 預篩範圍過寬(潛伏bug, 由本批新增
`activeFired` 首次暴露)

**發現**: 落地 `activeFired` 後跑真實資料 TRACE 抽驗(士爭先赴), 發現除了正確的
「（自身發動主動戰法觸發）發動」日誌行外, 士爭先赴還會在自己**受到攻擊**時額外多發動
一次(日誌顯示錯誤的「（受擊觸發）發動」標籤)。追查發現 `onHitTacs`(`sgz.py` 對應
`on_hit_tacs`)/`onHitEffectTacs`(`on_hit_effect_tacs`) 的預篩陣列過去只用
`t.when.on`(或 `e.when.on`) 是否為**真值**(truthy)判斷是否收錄, `onHit()`/`on_hit()`
內部迴圈也只顯式排除 `on==="attacked"` 且非普攻的情形, 對其餘任何 `on` 字面值(包括批27
新增的 `"dealtDamage"`、本批新增的 `"activeFired"`)一概放行當成 `"damaged"`(任意傷害
觸發)處理。

全庫過去只有 `attacked`/`damaged` 兩種戰法級 `when.on` 值, 從未真正踩中此漏洞; 效果級
`e.when.on` 過去除 `attacked`/`damaged` 外也有3筆 `dealtDamage`(深謀遠慮/白衣渡江/
非攻制勝), 這3筆**其實從批27上線起就已經受到影響**——它們的 `heal`/`disarm`/`silence`
效果除了透過正確的 `onDealEffectTacs` 反應式路徑觸發外, 還會在單位**受到任意傷害時**
被 `onHitEffectTacs` 誤判成 `damaged` 事件額外多結算一次, 造成比預期更高頻的雙重觸發
(白衣渡江: 受擊時也可能誤觸發繳械/計窮; 深謀遠慮/非攻制勝: 受擊時也可能誤觸發治療) ——
此問題規模比本批直接動到的士爭先赴更廣, 是全庫既有資料的隱性高估, 本批一併修復。

**修復**: `onHitTacs`/`on_hit_tacs`、`onHitEffectTacs`/`on_hit_effect_tacs` 兩處預篩,
從 truthy 檢查收斂為明確白名單 `{"attacked", "damaged"}`(`e.when.on in ("attacked",
"damaged")`), 只有這兩種事件值才會被收進「受擊反應式」預篩陣列——`dealtDamage`/
`activeFired` 各自有專屬的 `onDealTacs`/`onDealEffectTacs`/`activeFiredTacs`/
`activeFiredEffectTacs` 預篩與獨立事件掛鉤點, 不應該也落入受擊反應式路徑。

**驗收**: 修復後 `sgz.py` self-check 全數重跑仍通過(既有77/78等測試皆用獨立合成 `Unit`,
未觸發此雙重計算場景, 故修復前測試皆綠燈, 不代表沒有bug——這正是為何需要跑**真實資料**
TRACE 抽驗才發現此問題, 純合成單元測試對「多個 `when.on` 值共存於同一 `Unit`」的交叉
污染場景覆蓋不足的教訓)。真實資料 TRACE 抽驗(node 直接呼叫 `engine.js` 的
`buildPool`+`trace()`, 樂進持有真實 `type:"active"` 戰法臨戰先登 + 傳承士爭先赴, 50場
戰鬥400個回合逐回合核對): 修復前「士爭先赴」日誌行同時出現「（自身發動主動戰法觸發）」
與「（受擊觸發）」兩種標籤(雙重觸發); 修復後 400 回合中僅出現「（自身發動主動戰法觸發）」
標籤, 且每次出現都與同回合「發動戰法【臨戰先登】」共同出現(0 違規), 統計觸發率
(122/400回合中觸發, 且與臨戰先登實際發動次數的比值≈0.5)精確符合 `rate:0.5` 設計值。

**驗收基準(批31更新)**: 全庫 R1-R22 應維持零違規常態; `reparse_effects.py` 兩輪
byte-diff 為空(冪等); `sgz.py` self-check 全數通過(含新增86/87); `lint_tactics.py
--selftest` 全數通過(含 `CAPABILITY_INVENTORY_IGNORE` 新增 `damaged`, 因白名單化後
`"damaged"` 字面值首次出現在原始碼被 R20 drift 掃描器盤點到, 非新原語)。

---

## 28. 批32: `active` 型戰法補上 `t.when`/`e.when` 回合窗口檢查(含 parity) + `dealtDamage` 的
`targetSel` 支援 + `R23`(奇偶交替塌縮)/`R24`(虛弱誤建暈眩)/`R25`(傷害宣告零輸出) 三條新lint規則

**背景**: 盲測v15(68分)4筆0分全是早期解析沉積, 各對應一條未立的lint規則, 方法論同批25起「先立
規則→全庫掃→清零」。三規則初掃全庫328筆合格池恰各中1筆(飛沙走石/仁德載世/短兵相接), 清零後
全庫R1-R25零違規。另完成監統震軍(v15另一筆0分)的核心機制重建。

### A. `active` 型戰法的 `t.when`/`e.when` 回合窗口過去從未真正生效(R23根因)

**發現**: 飛沙走石(SP張寶)「奇數回合...偶數回合...」互斥交替, `e.when.parity` 早於批16就存在
且已用於武鋒陣/垂心萬物(皆 `type:"command"`), 但飛沙走石是 `type:"active"`——追查後發現:
1. `fight()` 主迴圈的 `active` 型戰法擲骰判定(`sgz.py` 原第1808行/`engine.js` 原第1375行)
   從未讀取 `round_ok(t0, rnd)`(`command`/`passive` 分支一直都有此檢查), 故頂層 `t.when`
   對 `active` 型戰法從未真正生效過(不論 rounds/from/until/parity/every)。
2. `apply_effects()`/`applyEffects()` 對非heal、非everyRound效果, 除了「母戰法無 `t.when`
   時 prep 階段的 skip-and-defer-to-scan」邏輯外, 完全沒有檢查 `e.when` 本身——而該「回合迴圈
   通用掃描」(見第6.4節)只覆蓋 `passive`/`command` 型戰法, **不含 `active`**。故 `active` 型
   戰法即使補上 `e.when.parity`, 在 `fight()` 主迴圈 `apply_effects(u, active_dst, t, ...)`
   直接呼叫時(無 `skip_when_effects`)也會被無條件套用, 完全忽略 `e.when` 指定的視窗。

**修復**: (1) `active` 型戰法擲骰判定補上 `round_ok(t0, rnd)`(對稱 `command`/`passive` 既有
判斷), 對唯一既有帶 `t.when` 的 active 戰法(移花接木, `when` 僅含 `dur` 鍵)無回歸(`round_ok`
對未知鍵一律回傳true)。(2) `apply_effects()`/`applyEffects()` 新增通用 `e.when` 檢查: 任何
非heal、非everyRound效果只要帶 `e["when"]`, 套用前一律用 `round_ok` 驗證當前回合是否落在
視窗內(不論呼叫路徑, 不限 `passive`/`command`), 不符合則跳過該效果段。此檢查與既有各路徑
(prep 的 skip-and-defer / heal_only 常駐 / 回合迴圈通用掃描)並存不衝突, 因為那些路徑要嘛
會在更早的判斷式攔截(不會走到這個新檢查), 要嘛壓根不會呼叫到 `apply_effects`。

**飛沙走石重建**: 奇數回合效果(智力debuff + 水攻dot, 各 `e.n:2`)補 `e.when:{parity:"odd"}`;
偶數回合效果(沙暴dot + 武智互換, 各 `e.n:1`)補 `e.when:{parity:"even"}`; `swap` 額外修正
`n`(無→1, 精確對應原文「敵軍隨機單體」, 取代先前誤套全體敵軍的範圍錯位)。全戰法**不**使用
頂層 `t.when`(避免與其中一組效果的 `e.when` 衝突, 見第6.4節既有限制), 純靠上述新增的效果級
通用檢查各自獨立判定。驗證: node TRACE 60場戰鬥, 奇數回合僅見智力debuff+水攻(2人), 偶數回合
僅見沙暴+武智互換(單體), 零跨奇偶洩漏(見批32報告)。

**義膽雄心(同類但未修復, 記錄供未來參考)**: 原文同為奇偶交替(單體兵刃傷害+降武力 vs 群體
謀略傷害+降智力), 但 `type:"passive"`, 且核心單體傷害段是頂層 `coef`(而非可獨立掛 `e.when`
的效果)——若要用 `t.when.parity` 精確表達奇數回合的 `coef` 觸發窗口, 會與偶數回合效果(`dot`/
`stat`, 非heal)的 `e.when.parity:"even"` 產生第6.4節衝突: `passive`/`command` 型戰法一旦有
`t.when`, 其效果只能透過「`t.when` 視窗開啟時一次性套用」的 `when_fired` 去重路徑(見1721-1750
行)結算, 該路徑對 `rounds` 外的視窗(如純 `parity`, 無 `rounds` 清單)是**整場戰鬥只觸發一次**
(`when_fired` 無條件加入, 不像 `heal_only` 通道有 rounds/from-until 之分), 而非每個符合條件
的回合重新觸發——若強行套用會比現況「兩者同時常駐生效」的近似更嚴重劣化(從「兩倍高估」變成
「後半場几乎不觸發」)。維持既有近似(已誠實揭露)不動, 需要更完整的「`passive`/`command` 型
戰法的視窗式效果比照 `heal_only` 支援 rounds vs from/until 的差異化去重」引擎升級才能安全修復,
非本批範圍。

### B. `dealtDamage` 的 `coef` 傷害段補 `targetSel` 支援

**發現**: 監統震軍(沮授)「普攻後對負傷最高之敵造成謀略傷害」——`dealtDamage`(批27新增)的
`coef` 傷害段過去固定命中觸發本次事件的同一目標 `dst`(如「普攻打誰, 就對誰額外打一次」),
沒有讀取 `t.get("targetSel")`, 無法表達「選標準則與觸發目標無關」的獨立選標語意(原文的謀略
傷害目標是「負傷最高之敵」, 與普攻打中的目標是兩件不相關的事)。

**修復**: `dealt_damage()`/`dealtDamage()` 的 `coef` 傷害段判斷式補上 `t.get("targetSel")`
分支(比照主動戰法主迴圈既有的 `targetSel` 判斷式, 見 `pick_by_criterion(foes_of(src),
t["targetSel"])`), 若戰法帶 `targetSel` 則改用準則選標, 找不到符合準則的目標時不出手(不誤
退回 `dst`)。無 `targetSel` 時完全維持既有行為(命中 `dst`), 向後相容全部既有 `dealtDamage`
戰法(白衣渡江/深謀遠慮/非攻制勝/士爭先赴等, 皆無 `targetSel`, 不受影響)。

**監統震軍重建**: 原「延長友軍施加的負面狀態1回合」被錯誤實作成 `amp(who:enemy, val:-0.38,
dur:99)`——`amp` 對 `who=="enemy"` 且 `val<0` 的實際分派是直接讓敵方單位自己的攻擊倍率-38%
常駐, 與原文「延長己方debuff時效」是完全不同方向的機制(敵方輸出降低 vs 己方debuff續期), 已
移除(非近似, 是錯誤機制替換, 該語意本身因無「debuff剩餘回合數延長」原語仍未建模, 已誠實
`_note` 揭露)。核心「自身普攻後對負傷最高之敵造成謀略傷害(114%)」改用
`t.when:{on:"dealtDamage", dmgType:"phys"}`(僅普攻/兵刃傷害觸發) + `t.targetSel:"mostDamaged"`
(=`minTroop`準則, 「負傷最高」=兵力損失絕對量最大=當前剩餘兵力最低, 語意正確、非6.5節/
17節提到的「兵力最高」缺口) + `coef:1.14`(kind:intel) 精確重建。「若敵軍無負面狀態則改為
治療」的條件分支(heal段)因引擎無「目標是否帶有任一負面狀態」的通用判斷(僅有 `ifTargetHas`
檢查單一指定狀態), 維持既有無條件常駐治療近似, 已誠實揭露。驗證: node TRACE 確認傷害穩定
命中兵力最低的敵方單位(而非普攻本身的隨機/固定目標), 與普攻目標各自獨立judged。

**R23(奇偶交替塌縮)**: 原文含「奇數回合...偶數回合...」/「單數/雙數回合」/「奇偶交替」等
明確互斥切分回合措辭, 但戰法/效果皆無 `when.parity`(戰法頂層或任一效果), 且無主題相關揭露
(要求提及 parity/奇偶/交替/單數/雙數等關鍵字, 純粹複誦原文的舊近似說明不算數)。全庫初掃1筆
(飛沙走石), 已重建清零。

**R24(虛弱誤建暈眩)**: 原文明確寫「虛弱」且緊鄰「(無法造成傷害)」註解(本庫對此詞的固定官方
定義: 只封鎖傷害輸出, 不影響行動/其他效果觸發), 但落地效果用了 `k=="stun"`(震懾, 全禁一切
行動, 威力遠大於虛弱), 且無主題相關揭露(刻意排除裸「虛弱」/「無法造成傷害」當豁免關鍵字,
這兩詞是原文本身固定伴隨用語, 任何複誦機率數字的舊近似說明天然都會含有, 卻完全沒有承認
「stun本身是錯誤機制替換」——這正是仁德載世批28遺留案例的陷阱, 也是本規則的設計初衷; 只接受
明確點出「stun/amp/全禁/震懾」等機制層級對比詞彙的揭露)。慣例: 「虛弱(無法造成傷害)」=
`amp(val:-1)`(對 `who="enemy"` 走 mitig 分支全額封鎖傷害輸出, 見批23乘敵不虞/克敵制勝/
威謀靡亢/臨戰先登既有慣例), 不得升格用 `stun` 頂替。全庫初掃1筆(仁德載世), 已重建(改用
`amp(val:-1, n:1, e.rate:0.10)`, 主將時機率提升至12.5%-25%因無「身份縮放機率」原語仍未
建模, 見第9節同類缺口, 已誠實揭露)。

**R25(傷害宣告零輸出)**: 原文明說「造成兵刃/謀略攻擊/傷害」但 `t.coef==0` 且無 `extraHits`
傷害段、無 `choices` 傷害分支、無 `dot` 傷害段承載, 導致核心傷害輸出完全掛零, 且無主題相關
揭露。低誤報設計: 只抓「(對|向)...造成...攻擊/傷害」或「(再次)?發起...攻擊/傷害」這類明確
宣告「本戰法對目標輸出一次傷害」的句型, 排除「無法造成傷害」(虛弱類負面狀態描述, 非本戰法
自身輸出)與「使...造成(的)?傷害提高/提升」(amp增傷描述, "造成傷害"是被修飾的受詞而非本戰法
直接輸出的傷害宣告, 如乘勝長驅/長驅直入/威武並昭皆屬此類假陽性, 已排除)。全庫初掃1筆(短兵
相接), 原文無任何百分比/點數數值, 已比照同結構近義戰法「短兵相見」(`coef:2.1`, `stat.add:
-30`, 皆滿級值)重建, `_est:true` 誠實標註。

**驗收基準(批32更新)**: 全庫 R1-R25 應維持零違規常態; `reparse_effects.py` 兩輪byte-diff為空
(冪等); `sgz.py` 全數自我測試通過(含新增88: `dealtDamage`+`targetSel` 端到端 `fight()` 驗證);
`lint_tactics.py --selftest` 全數通過(60案例, 含R23/R24/R25各自陽性/陰性樣例)。

---

## 附錄: 全部引擎支援的效果原語(k)一覽

供對照哪些機制已有原語、哪些仍是缺口。見 `sgz.py` `apply_effects()` 的 k 分支
(792-1051行)與 `lint_tactics.py` 的 `PER_KIND_FIELDS`。

`heal` / `settle` / `redirect` / `amp` / `mitig` / `stun` / `silence` / `disarm` /
`chaos` / `ambush` / `insight` / `immune` / `first` / `stat` / `dot` / `extra` /
`stack` / `decay` / `swap` / `pierce` / `counter` / `taunt` / `shield` / `dodge` /
`surehit` / `healblock` / `lifesteal` / `rateup` / `chargeup` / `healBoost` /
`healGiven` / `fakeReport` / `dispel` / `block`(批22新增, 次數型格擋, 見第11節)

戰法/效果級輔助欄位: `when`(rounds/from/until/parity/every/on/hpBelow/hpAbove) /
`targetSel` / `ifTargetHas` / `scale` / `choices` / `extraHits` / `everyN` /
`hitsRepeat` / `lockTarget` / `undispellable` / `nativeOnly` / `inheritedOnly` /
`prepOnly` / `e.n`・`e.nMax`(批23新增, 見下方說明) / `e.rate`(批23新增, 見下方說明) /
`e.ifLeader`(批26新增, 見上方第20節, 施放者須為隊伍主將index 0才套用該效果段) /
`stack.stackPer`(批26新增, 見上方第20節, "round"預設逐回合遞增/"cast"每次發動遞增) /
`when.on:"dealtDamage"`(批27新增, 見上方第21節, 施放者造成傷害後反應式觸發, 對稱於既有
`"attacked"`/`"damaged"`, 選填 `when.dmgType` 區分兵刃/謀略觸發條件) / `choices` 現已
對 `command`/`passive`(批27 B)與 `on_hit()` 反應式路徑(批27 C)全面生效, 不再限於
`active`/常駐輪詢派發路徑(見上方第21節/§8/§18a) /
`counter.guardFor:"leader"`(批28新增, 見上方第22節, 守護式反擊, A受擊時B代為反擊) /
`normalOnly`(批28新增, `amp`/`mitig`/`redirect` 效果欄位, 見上方第22節, 限定只對
普通攻擊生效/受影響, 戰法/突擊傷害不受影響) /
`e.everyRound`(批30新增, 見上方第26節A項, 非heal效果的逐回合重擲通道, 任何 `k` 皆可掛,
只在 `heal_only`/`healOnly` 常駐路徑套用, 對稱於既有 `heal` 逐回合重擲慣例) /
`who:"sub1"/"sub2"`(批30新增, 見上方第26節C項, 副將固定位置選標, 對稱於既有
`who:"leader"`=`allies[0]`, `sub1`=`allies[1]`/`sub2`=`allies[2]`) /
`when.on:"activeFired"`(批31新增, 見上方第27節A項, 施放者自身成功發動主動/突擊戰法後
反應式觸發, 對稱於既有 `"attacked"`/`"damaged"`/`"dealtDamage"`, 只覆蓋「自身」發動這一
方向, 選填 `when.timing`(`"before"`/`"after"`, 引擎不細分兩者結算順序)) /
`e.activeOnly`(批31新增, 見上方第27節B項, `amp` 效果欄位, 限定只對主動戰法造成的
傷害生效, 對稱於既有 `normalOnly`; 【批40 B訂正】批31 A原文誤稱涵蓋「突擊」傷害, 實作
上 `fight()` 主迴圈突擊擲骰呼叫點過去確實誤傳 `is_active=True` 導致突擊傷害誤觸
`activeOnly`, 批40 B已修正呼叫點改傳獨立的 `is_charge`, `activeOnly` 現只對真正
`type:"active"` 戰法傷害生效) /
`e.chargeOnly`(批40新增, 見下方第38節B項, `amp` 效果欄位, 限定只對突擊戰法造成的傷害
生效, 對稱於 `activeOnly`, 兩者現為互斥分類) /
`extraHits.ifSameTargetIsLeader`(批31新增, 見上方第27節C項, 事後過濾主段隨機選定的
`sameTarget` 目標是否恰為敵軍隊伍固定主將位 `foes[0]`) /
`when.parity` 對 `type:"active"` 戰法生效(批32新增, 見上方第28節A項, 過去頂層 `t.when`/
效果級 `e.when` 對 `active` 型戰法從未真正檢查回合窗口, 現與 `command`/`passive` 同步支援
rounds/from/until/parity/every) /
`dealtDamage` 的 `targetSel`(批32新增, 見上方第28節B項, `coef` 傷害段可依準則選標, 不再
固定命中觸發本次事件的同一目標, 無 `targetSel` 時維持既有行為向後相容)

**批23新增: 效果級 `e.n`/`e.nMax`(A1) 與 `e.rate`(A4)** —— 過去非CTRL效果(`amp`/`mitig`/
`stat`/`dot`/`healblock`/`rateup`/`insight`/`extra`/`lifesteal`/`healBoost`/`healGiven`/
`pierce`/`surehit`/`dodge`/`shield`/`swap`/`fakeReport`/`immune` 等)的 `who:"enemy"`/
`who:"ally"` 無條件套用到全體敵軍/我軍, 大量原文寫「單體」「N人」的非控制效果被系統性
高估成全體(CTRL_K 家族 stun/silence/disarm/taunt/chaos 不受影響, 它們一直讀戰法頂層
`t.n`/`t.nMax`)。現在效果自帶 `e.n`(可配 `e.nMax`)時, 比照 CTRL_K 群體選標邏輯隨機挑
`e.n`(~`e.nMax`)個不重複目標(單體時優先鎖定當下 `tgt`); 無 `e.n` 時完全維持原行為(全體),
向後相容。`lint_tactics.py` R13 掃描全庫比對原文目標數描述與 `e.n` 是否一致。

同時, 效果級 `e.rate` 現在於 `apply_effects()` 通用套用(不限 `onHit`/`delayedEq` 兩條
路徑), 套用前擲骰判定, 見上方第4節。

---

## 29. 批34: 兵種營建築Lv10附贈戰法 —— 結構性缺口(無法接上任何Unit), 僅資料落地 【批36已接上引擎】

**發現於**: 批34, user城建卡截圖(槍兵營「破軍」/盾兵營「守禦」), 對照
`docs/data/calibration_anchors.json` 的 `troop_camp` 錨點(批33遺留待查項)。

**背景**: 三國志戰略版的五種兵種營建築(槍/盾/弓/騎/器械)升到Lv10時, 會附贈一個依所屬
兵種觸發的專屬戰法(槍兵營→破軍/盾兵營→守禦/弓兵營→齊射/騎兵營→疾馳/器械營→負重, 經
agy+grok雙查證, 詳見 `docs/data/tactics_parsed.json` 對應條目 `_note`)。這與現有引擎的
`CAMP`(`sgz.py` 全域常數, 目前=4, 全屬性平加)是完全不同的兩個機制——`CAMP` 只是「兵種營
帶來的固定屬性加成」的粗略近似(不分兵種/等級, 全員同享), 但「Lv10 附贈戰法」需要:
(1) 城建等級資料(各營目前等級), (2) 依 Unit 實際兵種(而非戰法本身)判斷是否應掛載對應
戰法的 attach 邏輯。

**引擎現況**: 全庫搜尋確認 `troopLimit`(戰法可用兵種限制)欄位從未在 `sgz.py`/`engine.js`
戰鬥邏輯中被讀取, 純粹是資料庫metadata; 沒有「本場戰鬥每個Unit的兵種類型」與「己方兵種營
等級」的資料流入戰鬥模擬器。這意味著即使把破軍/守禦等五個戰法寫入 `tactics_parsed.json`
(批34已完成, 供未來查閱與人工配將分析參考), **也無法讓模擬器自動判斷「這個Unit是槍兵
且己方槍兵營Lv10, 故應獲得破軍」**——現有的「戰法附加」模型是「每個General卡面自帶1個
innate戰法+人工選配的兵書/裝備/緣分」, 城建增益完全在這個模型之外, 是另一層資料(類似
賽季buff `season_modifiers.json`, 但連那個至少有「賽季」這個維度可以整批套用, 城建增益
需要「每個Unit各自的兵種」這個现有模型完全沒有的維度)。

**本批決策**: 只落地資料(五個戰法的官方原文/名稱/數值, 見 `tactics_parsed.json` 對應
`_note`/`_todo`), 不嘗試接上模擬引擎(需要城建等級資料模型+兵種判斷attach邏輯两層新基礎
建設, 超出批34「內容補全+錨點落地」的資料層範圍, 屬於未來若要做「城建配置」功能時的
前置需求)。五個戰法皆標記 `cat`/`src`:"BUILDING"(新分類值, 與既有INNATE/EVENT/
INHERITANCE並列, 純描述性、不影響任何戰鬥邏輯分派)供未來辨識。

**信度分級**: 破軍(槍兵營)/守禦(盾兵營)有user一手城建卡截圖佐證名稱, 效果數值來自agy/
grok查證(守禦的治療率agy兩次查證分別給126%/100%, 有出入, 已誠實記錄取保守值)；齊射
(弓兵營)/疾馳(騎兵營)/負重(器械營)僅有agy單一AI查證、無user截圖交叉驗證, 信度較低,
待未來user提供對應截圖後覆核(尤其騎兵營「疾馳」與早期查詢一度出現的「先攻」說法有分歧,
已在 `_note` 記錄兩種說法供覆核)。

**批36更新: 已接上引擎**。隊伍配置新增 `campLv`(0~10, 預設0=不啟用)。屬性面: 每級
+0.25%該兵種造成傷害, 走既有 `amp` 原語(與進階/典藏 `a.amp` 同慣例, 在 `Unit` 建構子
`adds` 初始化段推入, `src:"兵種營"` 標記), 全隊每個Unit都吃這份加成(這支是「隊伍造成
傷害」的隊伍級加成, 三合一另外兩支才是「單一持有者」機制)。全屬性+4(既有 `CAMP` 常數)
維持全域無條件套用不變, 未依 `campLv` 動態化(即 `campLv=0` 時仍照舊套用+4, 這是既有
簡化, 非本批新增缺口, 本批未改動 `CAMP` 常數本身)。

Lv10附贈戰法attach: 原文「我軍隨機單體/群體」/「我方全體」是一整隊只發生一次(或全隊一次
性共享), 而非「隊上每個Unit各自獨立擁有這個被動」——若照字面把戰法直接掛到隊伍每個Unit
身上(初版實作曾經這樣做), 3人隊會導致該戰法被觸發3次(如守禦3次群體治療≈3倍效果, 已在
本批開發過程中用鏡像對局TRACE抓到並修正)。最終設計: `fight()` 建構Unit前, `campLv>=10`
時隨機挑隊上1個index當「持有者」(`Math.floor(rnd()*team.length)`/`random.randrange`),
只有該Unit的建構子收到 `isCampHolder=true`(JS)/`is_camp_holder=True`(Python)才真正把
對應戰法 `push`/`append` 進 `this.tactics`/`self.tactics`(淺拷貝加 `_campBuilding:true`
標記供辨識), 該隊其餘Unit仍正常吃上述屬性%加成、但不重複attach戰法。兵種→戰法對照表:
`CAMP_TROOP_TACTIC = {槍:破軍, 盾:守禦, 弓:齊射, 騎:疾馳, 器:null}`(器械"負重"因
`type:"none"` 已被 `TACTICS` 載入時的 `t.type!=="none"` 過濾排除, 查表查不到自然不掛,
不需要額外特判)。

雙引擎(`docs/engine.js`/`sgz.py`)`Unit` 建構子/`fight()`/`trace()`/`simulate()` 皆已
加上 `campLv`(`campLvA`/`campLvB`)與 `isCampHolder` 對應參數, 皆為尾端新增可選參數,
預設0/false, 對既有全部呼叫點向後相容(未傳等同批34前行為)。`docs/app.js` UI新增
「兵種營Lv」下拉(0~10, 預設10——玩家後期普遍滿級), TRACE準備階段標頭顯示各隊campLv與
附贈戰法名。sgz.py `demo()` 新增96-101號assert(含雙引擎鏡像對局勝率方向驗證: 同隊
campLv=10 vs 0 對戰同隊, 有營一方勝率應提升, 已用固定隨機種子驗證+8~9個百分點量級,
方向與量級皆合理)。齊射(弓兵營)實測發現其 `passive+coef+t.when(rounds)+空effects` 的
資料形狀, 實際命中傷害的路徑是 `fight()` 主迴圈 order-loop(讀 `t0.coef`, 受
`u.silence` 閘門保護——持有者被計窮/震懾時齊射不會造成傷害), 而非prep/回合迴圈的
`t.when` 效果掃描分支(該分支只印TRACE、因 `effects:[]` 不造成傷害); 兩分支對同一戰法
各自獨立判斷 `when` 是否開啟, 故TRACE會看到兩行「發動【齊射】」(其一純日誌, 其二才是
真正命中), 此為既有戰法「用武通神」已存在的同族資料形狀共通現象(非本批引入的新bug),
已在 `tactics_parsed.json` 「齊射」`_todo` 記錄。

## 30. 批34: `block` 消耗無傷害門檻條件(機鑑先識「警戒」6%兵力門檻) 【批35已修正】

**發現於**: 批34, grok查證機鑑先識(SP荀彧)原文確認「受到的傷害超過自身可攜帶最大兵力的
6%時(最低100兵力)，使該次傷害降低20%→40%…並消耗一次警戒」。

**批34現況(已過時, 見下方批35更新)**: `hit()`(`sgz.py` 787-792行)的 `block` 消耗邏輯是
「每次受擊必定消耗1次」(`if dst.block: block_val = dst.consume_block(); dmg *= max(0.0,
1-block_val)`), 沒有「僅當本次傷害超過某個門檻(依目標可攜帶最大兵力動態計算)時才消耗」
的條件判斷。批34評估多數中高傷害場景下影響有限, 暫不修正。

**批35更新: 已修正**。新增 `BLOCK_CONSUME_THRESHOLD = max(START_TROOP × 0.06, 100)` 常數
(`engine.js`/`sgz.py`), `hit()` 的 block 消耗判斷改為 `if dst.block.length && dmg >
BLOCK_CONSUME_THRESHOLD`(原始傷害dmg, 格擋套用前的值), 未達門檻(如本引擎常數
START_TROOP=10000下, 門檻=600)的傷害不消耗、不減傷, 照常全額打進去; 超過門檻才正常消耗
1次並套用減傷。已用 `sgz.py` 批35 test 90-94(自我測試90-94號, 見 `demo()` 函式)驗證低/
高傷害兩種情境的行為分流。

**影響評估**: 機鑑先識(警戒)在絕大多數對戰場景中, 敵方戰法/普攻的單次傷害量通常遠超過
「目標最大兵力×6%」這個門檻(現有校準錨點`damage_formula`顯示萬人部隊、同兵力對戰時單次
傷害約400~800量級, 遠高於6%門檻, 如10000兵×6%=600), 故批35修正後與批34「每次受擊必消耗」
的舊行為在多數中高傷害場景下**結果趨同**, 差異主要體現在極小額傷害場景(如持續傷害的尾段
殘值/極弱普攻/被大幅減傷後的殘餘量)——這些場景現在正確地不消耗警戒層數, 較批34版本更
精確(避免格擋層被無謂的小額傷害提前耗盡)。

## 31. 批34: 「受智力和魅力」雙屬性複合縮放(scale) —— 離月/魅月段, `scale` 僅支援單屬性

**發現於**: 批34, 離月(SP貂蟬)第②段「友軍受到治療時，有20%→40%機率(受智力和魅力影響)
使其受到傷害降低1.5%→3%(受智力影響)」——同一效果的觸發機率與減傷數值分別受**兩個不同
屬性**影響(機率受智力+魅力, 數值受智力), 且user戰報實錄(智力397/魅力225.32場次)單層
減傷7.36%, 用純`SCALE(智力397)×3%=5.55%`低估24%, 改用`SCALE(智力)×SCALE(魅力)`複合
相乘=7.53%, 與實錄誤差僅2.3%, 高度吻合原文「受智力和魅力影響」的雙屬性語意。

**與6.6節的區別**: 6.6節是「受X或Y最高一項影響」(單一屬性, 但要動態判斷挑哪個更高),
本節是「受X和Y兩者複合影響」(兩個屬性同時參與縮放, 很可能是相乘關係, 而非取一)——是
比6.6節更罕見的複合形態, 目前全庫僅離月一例確認需要此模式(其餘「受X和Y影響」措辭多半
可用6.6節的單屬性近似打發, 離月因有精確戰報數值才被抓出乘積關係)。

**本批決策**: `scale` 欄位維持只支援單屬性字串, 不新增雙屬性乘積語法(engine.js/sgz.py
`scale_of(caster, e["scale"])` 簽名改動涉及全庫超過100筆既有`scale`資料的相容性, 超出
「僅必要」範圍), 離月效果2的 `_note` 已誠實記錄複合縮放的推導與現況近似落差(維持
`scale:"intel"`單屬性, 保留mitig的EV折算val=0.06不變, 見 `tactic_corrections.json`
「離月」條目)。

## 32. 批34: 兵書「善戰」(四週年鏖戰季新增, 用間系)完全缺失

**發現於**: 批34, `calibration_anchors.json` 戰報錨點「善戰兵書=每回合+0.5%造成傷害
爬坡疊加(記錄累計7.00%)」/「善戰+0.5%/次累加3.50→6.50%再證」, 查庫發現 `data/bingshu.json`
(root, 唯讀)70筆兵書中**沒有「善戰」這個兵書**, 只有語意完全不同的自帶戰法「驍勇善戰」
(曹洪傳承主動戰法, 準備1回合兵刃攻擊+提速, 與兵書體系無關)。grok查證確認「善戰」是
「四週年鏖戰季新增的用間系小兵書」, 原文「自身每次造成傷害後，造成傷害提升0.5%，可疊加
8次」——與6.7節「疊加次數 vs push_add同源刷新」同族缺口(需要真正的「疊層計次」而非
「同源刷新」), 且屬於root data缺項(非既有戰法的建模偏差), 無法比照equips_parsed.json
新增「回元」的模式(equips_parsed.json不受reparse_effects.py管轄, 可任意手動新增/修改;
但bingshu_parsed.json同樣不受reparse_effects.py管轄——見下方決策)。

**本批決策**: `bingshu_parsed.json`(與equips_parsed.json同为手動維護檔, 不受
`reparse_effects.py` 管轄)已新增「善戰」條目, 用現有 `stack`(`stackPer:"cast"`,
per=0.005, max=8) 精確表達「每次造成傷害後+0.5%, 疊加上限8次」語意(`stack`只影響
`caster.amp()`自身增傷倍率, 與此戰法「自身每次造成傷害後,造成傷害提升」的自增傷語意
完全吻合, 且`stackPer:"cast"`遞增時機由 `apply_stack_cast()` 在「戰法本次成功發動」
呼叫, 但善戰的觸發時機是「造成傷害後」而非「戰法發動後」——兩者在多數情境結果接近
但非嚴格等價的近似, 已在 `_note` 誠實記錄, 詳見 `docs/data/bingshu_parsed.json`
「善戰」條目)。

## 33. 批34: 兵書「久戰」/「鬼謀」誤用 `amp`(固定自增傷)取代真正機制 —— 已修正

**發現於**: 批34, `calibration_anchors.json` 戰報錨點「久戰倒戈恢復4(比例遠小於刀出
如霆)」, 查證 `bingshu_parsed.json` 現行「久戰」條目為 `{"k":"amp","who":"self","val":
0.03,"dur":99}`(自身造成傷害恆定+3%), 與原文「自身獲得3%攻心和倒戈」的真實語意(造成
兵刃傷害時按傷害量3%回復兵力=倒戈, 造成謀略傷害時按傷害量3%回復兵力=攻心, 兩者皆為
`lifesteal`型機制, 非增傷)完全不同——`amp`(固定自增傷)是徹底的方向性錯誤(這個戰法根本
不提升傷害輸出, 只是低量吸血), 已修正為 `lifesteal` 原語(val=0.03, 不分dmgType=兵刃
謀略皆吃, 對應原文「和」字=兩者皆有非二選一)。grok查證的實測換算(133傷害→回復4≈133×
0.03)與修正後的 `lifesteal` 語意精確吻合。「鬼謀」(提高主動戰法造成的傷害, 現行
`{"k":"amp","val":0.12}`)語意本身正確(`amp`=增傷, 原文動詞「提高」對應), 但缺
`activeOnly`旗標(原文限定「主動戰法造成的傷害」, 現行對所有傷害皆生效, 高估——普攻/
被動/持續傷害皆不應受此加成), 已補 `e.activeOnly:true`(批31已有此原語, 見上方第27節
B項, 過去未在鬼謀套用是遺漏, 非新缺口), val維持0.12(原文無明確%數字, 沿用既有估計值,
battle_report錨點「+4.5%主動增傷」疑為含其他來源疊加後的戰報實測總和, 非鬼謀單一戰法
數值, 未強行覆寫)。均見 `docs/data/bingshu_parsed.json` 對應條目。

## 34. 批35: 狀態屬性曲線375族原語(scaleDiv/capVal) + block「準備階段鎖定」語意 + 兄弟掃描

**背景**: `docs/data/calibration_anchors.json` → `status_scale_375_20260704`(user 機鑑先識
警戒六點實測, 荀彧準備階段智力478.84~389.72)證實「狀態效果」(以block/警戒為代表)這一族
走除數375的獨立曲線(375點翻倍), 與傷害/治療/多數增減益類沿用的除數350不同, 且上限=
基礎值×2(40%→80%封頂, 非100%)。六點全部小數點後兩位精確吻合(見該錨點`fits`陣列)。

**A. 曲線族原語泛化**: 新增 `SCALE_G(v, div)`(除數可覆蓋, 預設350, 向後相容既有 `SCALE`)、
`scaleOf(caster, scale, scaleDiv)`/`scale_of(caster, scale, scale_div)`(第三參數透傳
`e.scaleDiv`)、`capValOf(v, capVal)`/`cap_val_of(v, cap_val)`(縮放後值上限clamp, `e.capVal`
選填, 不自動套用「基礎值×2」慣例, 逐效果顯式標)。全部向後相容: 未帶 `scaleDiv`/`capVal`
的既有資料行為零變化。

**B. 機鑑先識精確重建 + 準備階段鎖定**: `tactic_corrections.json`「機鑑先識」兩個 block
效果補 `scaleDiv:375` + `capVal:0.8`。並發現既有 `e.everyRound` 補層段(見批30)的
scale縮放值過去在**每次成功擲骰的當下**用 caster 當時即時智力重算, 與錨點`laws`
「數值鎖定準備階段: 裝備(出奇馬)在戰法計算前生效算入, 開戰後智力變動不影響」矛盾
——修正為新增 `lockedScaleOf(caster, e)`/`locked_scale_of(caster, e)`: 用「效果物件本身」
當快取鍵(`caster.scaleLock`/`caster.scale_lock`, Map/dict), 第一次掃描到該效果(即prep
階段, 不論該效果本身是否於prep就實際套用)即算定並鎖住縮放值, 之後(如everyRound補層段
延後到第2/3回合才擲骰命中)一律沿用鎖定值。目前只套用於 `k=="block"`(現階段唯一有實測
樣本佐證此語意的原語), 其餘 k(amp/mitig/stat等)維持現行「每次套用當下即時算」不變
(無對應樣本佐證是否同樣需要鎖定, 保守不擴大範圍)。TRACE(`effDesc`)同步顯示鎖定後的
實際值。

**C. 兄弟掃描結果**: 全庫掃描所有帶 `scale` 的 mitig/amp/pierce/redirect/block 效果
(狀態值類, 排除 rateup/chargeup——這兩者用完全獨立的 `RATE_SCALE_C` 曲線, 非本次曲線族
範圍; 排除dot/heal——分屬傷害coef/治療公式, 非本次「狀態值類%效果」範圍):
  - 46筆現有`scale`但無`scaleDiv`的效果(mitig 24/amp 19/pierce 2/redirect 1, 機鑑先識
    的2筆block已在B項處理不重複計) → 全數補 `_note2`「曲線族未定(350/375待實測), 現行
    沿用全域SCALE(除數350)預設」, 不擅自套用375(避免無實測樣本下錯誤地把全部效果強改成
    375曲線, 見紅線指示「不得擅自全改」)。
  - 32筆「raw effectText提及受X影響但目前效果無對應scale欄位」的候選, 逐一核對原文語境
    後歸類:
    - 26筆為**假陽性**(文字裡的「受X影響」實際修飾同一戰法的另一子句, 如傷害coef段/
      heal治療率段/機率觸發段, 並非該 mitig/amp/pierce/dodge/redirect 效果本身的magnitude,
      如乘敵不虞/仁德載世/克敵制勝的amp(-1)虛弱段、太平道法的amp(奇謀)段等) —— 已逐條
      核對raw effectText確認, 不新增scale。
    - 6筆為**結構性缺口**(raw effectText確實提及該效果段受某屬性影響, 但屬性來源本身
      超出現行`scale`欄位「單一固定屬性字串」的表達力, 或與既有註解直接矛盾, 無實測樣本
      可裁決): 扶危定傾(mitig, 受「自身最高屬性」影響=動態選最高的一項, 非固定屬性)、
      據水斷橋(amp, 受「雙方武力之差」影響=diff-based非單一caster屬性)、肉身鐵壁
      (redirect的兵刃傷害子類型受統率影響, 現行redirect無dmgType過濾)、聚石成金(mitig,
      條件式「僅黃巾陣營成員」才受統帥影響, 現行scale無條件式語意)、虎痴(amp, raw
      effectText明確提及受武力影響, 但既有`_note`聲稱「原文注明固定值不加scale」與此矛盾,
      未強行覆寫既有決策)、撫輯軍民(amp, 「受統率影響」子句文法上可能同時修飾造成/受到
      兩個子句, 亦可能只修飾後者, 歧義未決)。以上6筆均已在 `tactic_corrections.json`
      補 `_note2` 記錄發現與具體原因, **保守不新增scale欄位**(避免無錨點裁決下改變數值
      行為, 只揭露不動代碼/資料的既有語意, 待未來實測樣本或更明確的原始資料來源核對)。

**D. block消耗門檻**: 見上方第30節更新(已修正)。

**驗收**: `node --check docs/engine.js`/`python sgz.py test`(新增批35 test 90-94, 含六點
precise assert)/`python reparse_effects.py`(冪等, 二次執行輸出byte-identical)/
`python lint_tactics.py --selftest`(60項全過, `scaleDiv`/`capVal` 已登記進
`ENGINE_CAPABILITY_ALIASES`)/`python lint_tactics.py`(R1-R25全庫零違規, `capVal`/
`scaleDiv` 已加入 `KNOWN_EFFECT_FIELDS` 避免R9幽靈欄位誤報)。

## 35. 批37: `_topic_disclosed` 效果級精準化 + 停損決策過期重審制度化 + `stackPer:"attack"`/`when.normalOnly`

**A. lint 豁免精準化(`_topic_disclosed` effect scope)**: v16盲測實證兩案例(形一陣/撫輯軍民)——
豁免判定過去對「戰法內任何一則揭露文字」做全文子字串掃描, 兄弟效果的無關 `_note` 恰含主題
關鍵字(如"群體")就連帶豁免另一個效果的缺口。修法(lint_tactics.py `_topic_disclosed(p, kw,
effect=None)`): 傳入 effect 時改「效果級優先」——(1)效果自身揭露文字含關鍵字才直接豁免;
(2)戰法頂層文字須同時明確指涉該效果類別(`R_EFFECT_KIND_SELF_KW` k別名表)才算數; (3)完全
不再掃描兄弟效果的揭露文字。R1/R4/R6/R13/R17/R19/R24 呼叫端已傳入 effect scope(可窄化到
單一效果時), R2/R3/R5/R8/R10/R11/R12/R15/R16/R20/R21/R22/R23/R25 屬戰法整體級判斷維持
原掃描。附帶修正兩個被舊寬豁免掩蓋的規則窗口 bug: R1 的動詞窗口改「最近逗號分段」(功不唐捐
假陽性), R13 新增「全體緊鄰動詞宣告優先」判斷(`_r13_all_target_adjacent`, 大戟士假陽性)。
全庫重掃現形15筆(R1×4/R4×1/R13×9/R19×1), 全數清零(明細見批37報告/tactic_corrections.json
批37 A 註記)。selftest 新增3個 v16 回歸樣例(對舊邏輯驗證確實現形)。

**B. 停損決策過期重審**: 批次停損決策(「引擎不支援X, 不硬修」)會因後續批次補齊能力而過期,
過去無人回頭複核。本批:
- **義膽雄心重建**(批32停損「engine無法表達回合奇偶交替」過期): 頂層 `when:{parity:"odd"}`
  (main coef 只在奇數回合擲骰, passive coef dispatch 自批32讀 roundOk) + 三段效果
  `everyRound`+`e.when.parity` 互斥交替(奇: 武力debuff/偶: 謀略dot+智力debuff), dot 誤用
  no-op 的 `scale:intel` 改為 `kind:"intel"`(批23 A3通道)。
- **奮突重建**(批23停損「disarm無獨立prob欄位」過期): disarm 改
  `when:{on:"dealtDamage",normalOnly:true}`+`rate:0.35`(每次普攻命中後35%反覆判定);
  stack 補 `stackPer:"attack"`。
- **水淹七軍部分重建**: 主將加成用「基礎dot(0.96)+差額dot(0.12, `ifLeader:true`)」top-up
  拆法落地(原停損誤以為需要不存在的 ifLeader:false 變體); 項(2)(3)(`ifCasterStack`)複核仍成立。
- **新原語×2(雙引擎同步)**: `stackPer:"attack"`(第三種疊層遞增模式, 每次普攻確實命中造成
  傷害後+1層, 掛 dealtDamage 事件點頂端, 早於 onDealTacs 早退判斷; 繳械/震懾回合不誤疊層)
  + `when.normalOnly`(dealtDamage 反應式的觸發過濾旗標, 戰法級/效果級皆可, 限普攻傷害觸發
  ——`dmgType:"phys"` 無法區分普攻與兵刃戰法傷害)。
- **一次性清剿統計**: 停損措辭(不硬修/停損/強修會/暫不/保留現狀/超出本批)全庫掃描 13筆/9戰法,
  重建3(義膽雄心/奮突/水淹七軍項1), 複核仍成立6(十二奇策/同戰身先/義心昭烈/陷陣營/鷹視狼顧/
  知己知彼, 皆已補記「批37複核仍成立」+具體仍缺的原語名)。

**R26 決策: 不新增基於停損措辭的獨立規則**。理由: 該類措辭大量正當出現於「實測樣本缺口」
(非引擎能力缺口, 如陷陣營高順條件)與「已複核仍成立」的停損, 誤報難控。改為強化 R20:
(a) `NEGATION_KW` 補「無法表達」(義膽雄心 stale 句型的否定詞); (b) `ENGINE_CAPABILITY_ALIASES`
補「奇偶交替/回合奇偶」→when.parity、「普攻疊加」→stackPer:"attack"、「普攻後觸發/普通攻擊
之後」→when.normalOnly; (c) R20 新增歷史敘述排除(`R20_RESOLVED_KW`: alias 前後40字內含
「已過期/已重建/複核仍成立」等解決性措辭視為歷史引文, 不誤咬解決紀錄)。

**維護規約(停損註記)**: 今後寫停損/不硬修決策時, 註記**必須點名具體缺失的原語/機制名**
(如「需要 ifCasterStack」「需要跨單位事件廣播」), 而非只寫「引擎不支援」的泛稱——這讓 R20
的 alias 掃描在該原語未來落地、別名表登記後能自動抓到過期的停損; 同時新原語落地時**必須**
在 `ENGINE_CAPABILITY_ALIASES` 登記中文別名(R20-drift selftest 會強制)並順手 grep 全庫
停損註記是否有點名此原語的條目可以重建。解決一筆停損後, 解決紀錄可複誦舊措辭當歷史引文,
但須在引文前後40字內帶「已過期/已重建」等解決性措辭(R20 歷史敘述排除的判定窗口)。

**驗收**: `node --check docs/engine.js` / `python sgz.py test`(新增102-104: 義膽雄心
parity互斥交替/奮突欄位與reactive閘門/傳承奮突鏡像對局端到端) /
`node scratchpad/smoke_batch37.js`(103 asserts: 義膽雄心主傷僅奇數回合+dot僅偶數回合;
奮突disarm僅普攻後觸發, 主動兵刃傷害不觸發) / `python reparse_effects.py`(冪等) /
`python lint_tactics.py --selftest`(65項全過) / `python lint_tactics.py`(R1-R25全庫零違規)。

---

## 36. 批38: `when.who`("ally"/"otherAlly"/"enemy") —— 跨單位事件廣播基礎設施(全庫最大殘餘原語缺口)

**背景**: 歷輪盲測評估「跨單位事件監聽」是最大殘餘原語缺口(第15節「`allyAttacked` 廣播」、
第21節「敵軍/友軍發動戰法時」、第27節「activeFired 只覆蓋自身視角」皆點名同一族缺口, 影響
虎侯/十二奇策/經天緯地/神機妙算/舌戰群儒/騎虎難下等4-6+筆)。現有 `when.on`(attacked/damaged/
dealtDamage/activeFired)只監聽持有者**自身**發生的事件, 無法表達「任一友軍/敵軍發生某事件
時, 我(持有者, 可能是另一個單位)也跟著觸發」這類第三方旁觀語意。

### A. `when.who` 三值(選填, 效果級 `e.when.who`/戰法級 `t.when.who`, 預設未指定="self"向後相容零變化)

**設計**: 三個既有反應式事件點(`onHit`/`dealtDamage`/`activeFired`, `engine.js`/`sgz.py`
`on_hit`/`dealt_damage`/`active_fired`)全數擴充, 從「只掃描事件單位自己攜帶的反應式戰法/
效果」擴展為「額外掃描事件單位所在隊伍(`who:"ally"`/`"otherAlly"`)或敵對隊伍(`who:"enemy"`)
全體的同款反應式戰法/效果, 過濾出 `when.who` 匹配者才觸發」:

- `who:"ally"` —— 監聽對象是「事件單位所在隊伍的任一人(含事件單位自己)」, 持有者也必須在
  同一隊(廣播範圍限同隊, 不含對面)。如經天緯地/十二奇策「我軍全體發動主動戰法時」。
- `who:"otherAlly"` —— 同 `"ally"`, 但明確排除事件單位自己(對應原文「除自己之外的友軍...
  時」這類措辭)。如騎虎難下「當除自己之外的友軍受到普通攻擊時」。
- `who:"enemy"` —— 監聽對象是「事件單位所在隊伍的敵對隊伍任一人」, 持有者必須在敵對那一隊。
  如神機妙算/舌戰群儒「敵軍發動主動戰法時」。
- 未指定(或顯式 `"self"`, 非強制關鍵字, 只要非上述三值即視為自身路徑) —— 維持批31以前的
  既有語意, 只有「持有者===事件單位」才觸發, **零回歸**(全庫既有帶 `when.on` 但未帶 `who`
  的資料, 行為完全不變, 見驗收 107/士爭先赴回歸測試)。

**實作**: `engine.js`/`sgz.py` 各自新增 `broadcastHolders`/`broadcast_holders(evtUnit, who)`,
依 `who` 回傳候選持有者隊伍(`"ally"`/`"otherAlly"` → `alliesOf(evtUnit)`; `"enemy"` →
`foesOf(evtUnit)`)。三個事件函式(`onHit`/`dealtDamage`/`activeFired`)內部抽出共用核心
(`onHitFor`/`dealtDamageFor`/`activeFiredFor`, 對稱 Python `on_hit_for`/`dealt_damage_for`/
`active_fired_for`), 接受 `holder`(效果持有者, 可能不同於事件單位)與 `wantWho`(本次呼叫只
處理與此匹配的 `when.who`)兩個新參數: `whoOk(w)` 判斷式對 `wantWho` 未傳(自身路徑)只放行
無 `who` 欄位的既有寫法, 對 `"ally"`/`"otherAlly"`/`"enemy"` 只放行明確標記該值的新寫法(嚴格
字串相等, 不會互相誤放行)。`"otherAlly"` 額外用 `holder !== dst`(或 Python `holder is not
dst`)閘門收斂範圍(排除事件單位自己)。三個事件函式的呼叫端各自先跑一次「自身路徑」
(`wantWho` 未傳), 再各跑一次 `alliesOf(evtUnit)`(`"ally"`)+`alliesOf(evtUnit)`(`"otherAlly"`,
onHit/dealtDamage 才需要, activeFired 目前遷移案例皆為 `"ally"`/`"enemy"` 未用到 otherAlly
方向, 但實作對稱補齊供未來資料使用)+`foesOf(evtUnit)`(`"enemy"`)三組廣播掃描。

**節流**: 沿用既有 `hitFlags`/`hit_flags`(同回合每持有者每戰法/效果最多觸發1次), 鍵仍是
`t0`/`e` 物件本身, 廣播情形下鍵掛在 **holder**(觸發的持有者)身上而非事件單位, 與既有慣例
(節流以持有者為單位, 不以事件單位為單位)一致, 不會因為同一事件單位多次觸發同一持有者的
同一戰法而繞過節流。

**效能**: 每次事件多掃隊伍其餘1~2人的預篩陣列(`onHitTacs`/`onDealTacs`/`activeFiredTacs`
等, 絕大多數單位為空陣列, `O(1)` 提前退出), 3000場（`SGZ.simulate`, node）實測 287~354ms
(含多個廣播監聽者同場疊加的壓力情境), 遠低於500ms預算, 見驗收數字。

### B. 落地遷移(逐筆讀原文, 精確才遷)

| 戰法 | 事件+方向 | 結論 |
|---|---|---|
| 經天緯地 | `activeFired`+`who:"ally"` | 清潔1:1遷移(整戰法本體即此反應式監聽, 無並存自身獨立機制); 攻心段(基於傷害量回血)仍未建模, 見下方阻塞說明 |
| 神機妙算 | `activeFired`+`who:"enemy"` | 清潔1:1遷移(方向由批31「不同族, 不遷移」改為遷移); 主將分支條件判斷/智力差加成仍未建模(與本批無關的既有缺口) |
| 舌戰群儒 | `activeFired`+`who:"enemy"` | 遷移(方向由批31「不同族, 不遷移」改為遷移), 取代EV折算成永久buff的舊近似, dur由99改1(對應原文「持續1回合」) |
| 騎虎難下 | `attacked`+`who:"otherAlly"` | 遷移, 解決engine_limitations.md第15節點名的原始案例(「除自己之外的友軍受到普通攻擊時」) |
| 虎侯 | `attacked`+`who:"ally"`(部分) | 部分遷移: 原文是OR的兩個事件(「友軍發動或受到普通攻擊時」), 單一`t.when`只能選一個, 已選「受到」方向; 「發動」方向(`dealtDamage`)與「疊加5次」(stat原語無疊層計數能力)仍未建模, 已誠實記錄 |
| 十二奇策 | — | 複核仍成立, 不遷移: 「自己先施放買buff」與「(買到後)隊友之後發動觸發傷害」是兩個獨立事件的複合時序, 單一`t.when`只能表達其中一種, 需要「單戰法可同時掛自身cast閘門+cast後跨單位延遲監聽」的複合機制, 超出本批範圍 |
| 同戰身先 | — | 複核仍成立, 不遷移: 核心缺口是「rate自我遞減計數器」(每次發動後發動率降低10%)與「觸發者≠持有者」(隨機武將受普攻後由該武將自己反擊, 非固定持有者), 皆與跨單位事件廣播無關 |
| 機鑑先識 | — | 複核仍成立, 不遷移: 「我軍受到控制狀態時反向施加給敵方」需要全新「狀態施加」事件種類, 與本批擴充的三個既有事件(受擊/造成傷害/發動主動戰法)是不同觸發點, 見第25節第1類分類 |

**成功遷移6筆**(經天緯地/神機妙算/舌戰群儒/騎虎難下/虎侯[部分]/), **複核維持停損3筆**
(十二奇策/同戰身先/機鑑先識, 各自的阻塞點與本批`when.who`無關, 已在corrections的`_todo`
補記「批38複核仍成立」+具體阻塞原因)。

### C. 順帶快修(v16一分堆點名的可修項)

本批預算集中於A(引擎廣播原語)+B(逐筆遷移核對, 含全庫掃描發現的騎虎難下同族案例), C項
（v16一分明細的可用既有原語修快修）未額外新增獨立項目——B節遷移的6筆本身即涵蓋原v16-v21
歷輪多次點名"跨單位廣播"缺口的最大宗來源, 產出已優先分配至此。

**R20 能力別名登記**: `lint_tactics.py` `ENGINE_CAPABILITY_ALIASES` 更新「自帶主動戰法」/
「成功發動」條目(反映批38後「敵軍/友軍發動戰法時」不再一概視為未解決缺口), 新增「友軍發動
戰法」「友軍發動主動戰法」「我軍全體發動」→`activeFired`+`who:"ally"`、「敵軍發動主動戰法」
「敵軍嘗試發動」→`activeFired`+`who:"enemy"`。

**驗收**: `node --check docs/engine.js` / `python sgz.py test`(新增105-107: activeFired
`who:"ally"`/`"enemy"`廣播端到端驗證+`who`未指定回歸) / `node scratchpad/smoke_batch38.js`
(7 asserts: activeFired who:ally/who:enemy/who未指定回歸[士爭先赴真實資料] + onHit
who:ally/who:otherAlly[含排除自己驗證]) / `python reparse_effects.py`(兩輪byte-diff為空,
冪等) / `python lint_tactics.py --selftest`(65項全過) / `python lint_tactics.py`(R1-R25
全庫零違規) / 效能: `SGZ.simulate` 3000場 287~354ms(<500ms預算, 含多廣播監聽者疊加情境) /
三組對局TRACE歸因(node直接呼叫`SGZ.trace`): 對局1確認神機妙算(`who:"enemy"`)在敵方荀攸
發動十二奇策時觸發; 對局2確認經天緯地(`who:"ally"`)在隊友荀攸發動十二奇策時觸發; 對局3
確認騎虎難下(`who:"otherAlly"`)在隊友受普攻時觸發152次, 持有者自己受普攻時0次誤觸發(不
應觸發自己)。

## 37. 批39: 象兵三段重建(`ifTargetHas`自查+`everyRound`) + `when.dmgType`對稱補全
(`onHit`/`attacked`/`damaged`路徑) + `R26`統領揭露一致性防線 + 萬軍奪帥五態拆分

**背景**: v17盲測(66分)點名象兵(全庫唯一0分)三段機制(受傷延遲結算+灼燒自查條件式群攻/
混亂+蠻族統領陣營計數減傷)幾乎全數缺失; 全庫核對「若/由XX統領」條件式加成措辭(12筆既有
precedent)發現4筆(丹陽兵/白毦兵/白馬義從/先登死士)完全漏揭露, 新增lint規則後另掃出藤甲兵
(第5筆); `when.on:"damaged"/"attacked"`反應式路徑自批16建立以來從未支援`dmgType`過濾(對比
`dealtDamage`自批27就有), 剛勇無前/剛烈不屈「受到兵刃傷害時」誤及謀略傷害; 萬軍奪帥原文
5種獨立輕度狀態判定(遇襲/破壞/禁療/計窮/繳械, 各自22.5%→45%機率獨立擲骰)被合併塌縮成單一
`stun`(嚴重放大, stun完全鎖行動遠強於任一單項輕度狀態)。

### A. 象兵三段機制重建

**段1(受傷延遲結算+減傷)**: 沿用既有`mitig`+`_approx:"settle-ev"`近似(engine `settle`原語
僅支援攻擊向延遲傷害, 無法表達防禦向延後受傷機制, 批32已定案, 本批不動)。

**段2(自身有灼燒狀態時，獲得50%群攻及混亂狀態)**: **可用既有原語精確表達**——`ifTargetHas`
(批16新增, 原本設計給「效果/extraHits段: 只對已有該狀態的目標生效」)在`who:"self"`場景下
`dests=[caster]`先選定, 再被`ifTargetHas`過濾, 天然等價於「自查自身狀態」條件(無需任何引擎
擴充, 純粹是既有原語的新組合用法)。`everyRound`(批30新增)確保灼燒到期/消失時逐回合重新判定
(而非prep套用一次後`dur:99`常駐)。落地: `extra`(群攻, val:0.5, 同瞋目橫矛/矢志不移既有
"群攻無濺射原語, 以extra額外傷害輸出近似"慣例)+`chaos`(混亂, 本體即是"獲得混亂狀態"的字面
效果, 非常見的施加給敵方而是自身狀態)兩條效果皆帶`ifTargetHas:"dot"`+`everyRound:true`。
"50%"讀作群攻的傷害率修飾詞(非機率, 比照矢志不移"50%機率獲得群攻"vs瞋目橫矛"群攻...傷害率
35%→70%"的既有辨析慣例, 象兵原文無"機率"字樣, 與瞋目橫矛同款讀法), 故不設`e.rate`(灼燒
存在即無條件套用, 不再額外擲骰)。

**段3(若統領為蠻族，部隊每多1名蠻族武將結算的傷害額外降低5%→10%)**: **無法建模, 已揭露**。
現有`teamGate`(批24)僅支援`allDiff`/`allSame`兩種布林判定(隊伍陣營是否兩兩不同/皆相同),
無「數出隊伍中特定陣營人數, 依人數N線性縮放某效果數值」的計數型條件原語, 且「統領為蠻族」
本身是「主將所屬陣營=特定陣營」條件(現有`ifLeader`只判斷「施放者是否為主將」, 不判斷主將
陣營)。`_todo`已點名需要`teamGate`擴充為回傳陣營人數統計, 或新增
`ifLeaderFaction`+`countTeammatesOfFaction`組合原語。

### B. 「若/由XX統領」揭露一致性 + `lint_tactics.py` `R26`新規則

**全庫核對**(12筆既有precedent + 全庫掃描): 大戟士/無當飛軍/虎衛軍/陷陣營/西涼鐵騎/錦帆軍
已妥善揭露(`_todo`點名特定武將條件); 虎豹騎用`chargeup.leaderBonus`(批26曹純特例硬編碼
力²公式)精確建模, 非揭露而是已解決; **丹陽兵/白毦兵/白馬義從/先登死士 4筆完全漏揭露**
(任務背景點名)。逐筆核對`leaderBonus`是否可套用: `leaderBonus`是`k==="chargeup"`專屬的硬
編碼機制(只處理「特定武將=突擊發動率額外受武力影響」這一種形狀), 4筆分別是mitig值微調
(丹陽兵)/coef微調(白毦兵)/rateup條件式scale(白馬義從)/stack.max微調(先登死士), **皆非
chargeup, 無法套用leaderBonus**, 全數補`_todo`(需要新的「ifLeaderIsGeneral:XX」通用效果級
條件閘門)。

**`lint_tactics.py` `R26`(戰法級全文掃描慣例, 同R3/R5/R8/R16/R20, 非R13/R25式效果級窄化,
因「統領」條件通常修飾整個戰法的某個數值調整而非對應單一固定k類別)**: 原文含「若/由X統領」
(排除純陣營泛稱如「若統領為蠻族」, 用「陣營/蠻族/異族/同族/統領為」等關鍵字濾掉, 避免與
象兵段3的陣營計數缺口混淆——那是完全不同的缺口, 不歸R26管轄), 但戰法/效果皆無`leaderBonus`
且`_topic_disclosed`(全文掃描"統領"/"leaderBonus"關鍵字)未命中, 判定違規。全庫掃描時**額外
抓出藤甲兵**(若兀突骨統領, 損失兵力降低250%<300%, 過去`_note`只揭露了灼燒自損懲罰本身未
建模, 完全遺漏兀突骨條件那一句)——非任務原定4筆之一, 屬lint新規則上線後的額外收穫, 一併補
`_todo`。**5筆補齊後全庫R26零違規**。

### C. `when.dmgType`(戰法級/效果級)在`damaged`/`attacked`反應式路徑生效

**設計**: `hit()`的兩個`onEvent`呼叫點(dodge分支歸零傷害/正常結算後)補傳`kind`(本次傷害
類型, 尾端新增參數, 向後相容既有呼叫點——`hit()`內部已知`kind`, 純粹是之前忘記往下傳)。
`onHit`/`on_hit`簽名新增`kind`參數(尾端, 預設`undefined`/`None`), 內部新增`dmgTypeOk`/
`dmg_type_ok`(`!dt || dt === kind`, 與`dealtDamage`的`dmgTypeOk`完全同款), 套用到4個檢查點
(`onHitTacs`戰法級/`onHitEffectTacs`效果級/`onHitEq`裝備效果級/`onHitBs`兵書效果級), 對稱
`dealtDamage`自批27就有的`when.dmgType`過濾, 修正過去`damaged`/`attacked`反應式路徑完全
不分兵刃/謀略傷害觸發的缺口。

**資料遷移**: 全庫掃描「受到(的)?(兵刃|謀略)傷害(時|後)」句型, 命中4筆(丹陽兵/剛勇無前/
剛烈不屈/絕地反擊), 逐筆核對後**只有剛勇無前/剛烈不屈**實際走`when.on`反應式路徑(丹陽兵用
`mitig`+`dmgType`近似, 絕地反擊用EV折算的`stat`常駐疊加近似, 兩者皆非`when.on`反應式,
不在本節管轄範圍)。剛勇無前(`reparse_effects.py`的`ON_DAMAGED`白名單自動填`when.on`,
新增對應`ON_DAMAGED_DMGTYPE`白名單同步補`dmgType:"phys"`)+剛烈不屈(`tactic_corrections.json`
`set.when`直接補`dmgType:"phys"`)皆補上, 對應原文「受到兵刃傷害後/時」限定。全庫另核對
`ON_ATTACKED`四筆(氣凌三軍/後發制人/眾動萬計/魅惑)皆為「受到普通攻擊時」(攻擊手段限定,
非傷害類型限定), 不適用`dmgType`, 不誤加。

### D. 萬軍奪帥五態拆分重建

**設計**: 原文「22.5%→45%機率使其進入遇襲、破壞、禁療、計窮、繳械狀態，每種狀態獨立判定」
過去被合併塌縮成單一`stun`(整段用一個機率+一個狀態近似5個獨立判定, 嚴重放大——`stun`完全
鎖行動, 遠強於任一單項如`ambush`遇襲的單純行動遲滯)。核對`choices`(批16, 戰法欄位, 依權重
隨機選「一個」分支)語意后确认**不適用**——原文是「5個獨立判定」(可能0~5個同時發生), 非
「5選1擇一發生」, `choices`的互斥語意與此不符。改用`e.rate`(批23 A4起, 任何`k`皆可在
`active`主動擲骰命中後由`applyEffects`統一補上獨立擲骰判定, 見`opt.rateChecked`閘門)為每個
狀態各自掛`rate:0.45`(滿級值, 本庫慣例), 4個效果(`ambush`/`silence`/`disarm`/`healblock`)
各自獨立成`effects[]`條目取代原本單一`stun`。**目標選取**: `silence`/`disarm`屬`CTRL_K`
(見`engine.js`/`sgz.py` `CTRL_K`判斷式), 無`e.n`時預設`t.n=1`且優先鎖定`tgt`(=速度最快
單體, 主戰法既有選標); `ambush`/`healblock`非`CTRL_K`, 需顯式補`e.n:1`才會鎖定`tgt`而非
退化成全體敵軍(同既有`stat`/speed效果的`_note`反例, 見該效果既有揭露)。**「破壞」(禁用裝備)
狀態無對應原語**, 與決水潰城既有`_todo`結論一致(引擎完全無「禁用裝備」機制), 補`_todo`
點名, 維持未建模。

### 驗收

`node --check docs/engine.js` / `python sgz.py test`(新增108-111: 象兵ifTargetHas自查+
everyRound端到端/R26統領揭露5筆真實資料抽驗/onHit dmgType過濾端到端[剛勇無前受謀略傷害
不觸發、受兵刃傷害觸發]/萬軍奪帥五態拆分結構+獨立rate驗證) / node ad-hoc驗證腳本(20 asserts,
與sgz.py assertion對稱交叉核對兩引擎行為一致, 含`buildPool`+`Unit`+`applyEffects`+`hit`直接
呼叫) / `python reparse_effects.py`(兩輪byte-diff為空, 冪等) / `python lint_tactics.py
--selftest`(69項全過, 含新增R26四組陽性/陰性樣例) / `python lint_tactics.py`(R1-R26全庫零
違規) / 三組TRACE歸因(node直接呼叫`API.trace`): 對局1(夏侯惇inherit剛勇無前+剛烈不屈 vs
諸葛亮/周瑜/司馬懿)確認僅受擊觸發、剛烈不屈額外段兵刃傷害正確結算; 對局2(張飛inherit象兵,
騎兵)確認`兵種`類別mitig段正常套用; 對局3(呂布inherit萬軍奪帥, 弓兵 vs 劉備/諸葛亮/關羽)
20場TRACE統計遇襲96次/計窮788次/繳械236次/禁療66次/速度66次, 全數獨立觸發, 全庫掃描`stun`/
`震懾`關鍵字0次殘留(確認merge-bug已消除); 象兵ifTargetHas 200次試驗交叉驗證: 無灼燒時
extra/chaos觸發次數皆為0, 有灼燒時皆為200(無條件觸發, 非機率式, 符合原文"50%"是群攻傷害率
修飾詞而非啟動機率的判讀)。

## 38. 批40: `ofDamage` 支援屬性縮放(scale/scaleDiv) + `e.chargeOnly`(對稱`activeOnly`) + 分利/剛毅稽核

### A. `heal` 效果的 `e.ofDamage` 支援 `e.scale`/`e.scaleDiv`

**背景**: 批33新增 `e.ofDamage`(傷害比例治療, 見草船借箭)當時是固定比例常數, 不支援
屬性縮放。user 兩場對照實測(白字標統率戰報, 見 `calibration_anchors.json`
`caochuan_command_experiment`)證實草船借箭的治療比例本身仍「受統率影響」, 且縮放曲線
與既有350(傷害/治療屬性公式)、375(狀態效果, 批35)兩條曲線都對不上, 是第三條獨立曲線
(除數≈266.3)。

**設計**: `ofDamage` 結算時若效果帶 `e.scale`, 比例改為 `ofDamage×(1+(施放者
prep鎖定屬性-100)/scaleDiv)`。「prep鎖定屬性」沿用批35 B `lockedScaleOf`(準備階段第一次
掃描到該效果時算定並鎖住, 之後不因戰鬥中屬性浮動重新計算, 與 heal 屬性公式/block 的
準備階段鎖定同一慣例)。無 `e.scale` 時 `scaleMult=1`(向後相容既有純固定比例資料)。

**遷移**: 草船借箭 `ofDamage` 從 `0.355`(批39.5, 已作廢) 改為 `0.2716` +
`scale:"command"` + `scaleDiv:266.3`。兩點錨點驗證: 統率506.18→68.6%(實測67.5~68.7%)/
統率237.65→41.2%(實測41.2%精確命中)。**user裁決(2026-07-07)**: 批39.5「35.5%不受統率
影響」樣本經覆核為戰報無法回查、來源歸屬不明的舊場資料, 已作廢不採用; 曲線以兩張白字標
統率戰報兩點定案, 不再等待第三點確認。

### B. `e.chargeOnly`(amp 效果欄位, 對稱既有 `e.activeOnly`)

**背景**: 批31 A 新增 `e.activeOnly` 時, 呼叫端(`fight()` 主迴圈)把「主動戰法」與
「突擊戰法」兩個遊戲機制上互斥的分類都標記 `isActive=true`(JS)/`is_active=True`
(Python), 導致 `activeOnly` 標記的加成(語意應限定「僅主動戰法傷害」, 如士爭先赴)
被突擊傷害誤觸發。user戰報實測「一鼓作氣: 突擊戰法造成傷害提升12.00%」與「藏刀:
突擊戰法造成傷害降低5.00%+發動率+3%」證實這兩支兵書的措辭明確只講「突擊戰法」——
過去的舊揭露誤稱「引擎無此原語」是 stale 說法(activeOnly 原語本身已存在, 只是語意
被混淆成涵蓋突擊, 而非真的缺原語)。

**設計**: 新增 `isCharge`/`is_charge` 參數, 與 `isActive`/`is_active` 平行穿透
`hit()→damage()→amp()/addbonus()`(對稱 `normalOnly`/`activeOnly` 的既有穿透路徑)。
效果級新增 `e.chargeOnly:true`(僅 `amp` 支援), 結算時只在 `isCharge===true` 才計入。
修正 `fight()` 主迴圈突擊擲骰呼叫點: 過去傳 `isActive=true`, 改傳
`isActive=undefined`(不再觸發 `activeOnly`)+ `isCharge=true`(觸發 `chargeOnly`)。
`activeOnly`/`chargeOnly` 現為互斥分類, 各自對應「主動戰法」「突擊戰法」。

**遷移**: 一鼓作氣(兵書)`amp val:0.12` 補 `chargeOnly:true`。藏刀(兵書, 過去
`effects:[]` 空白未建模)新增兩條: `chargeup val:0.03`(突擊發動率+3%) + `amp
val:-0.05, chargeOnly:true`(突擊傷害-5%)。`lint_tactics.py` R20 別名表新增
「突擊戰法造成的傷害」/「突擊戰法造成傷害」→ `chargeOnly` 條目。

**迴歸測試**: `sgz.py` selftest 新增86b-2(chargeOnly只在is_charge時生效, 不誤觸
active/normal)/86b-3(activeOnly不再被charge傷害誤觸, 批31 A遺留bug的迴歸防線)。
兩項皆用多次取平均(消除 `damage()` 內建±4%隨機帶造成的單次取樣flaky, 曾實測單次
比較約18%機率因隨機噪聲誤判失敗)。

### C. 「分利」兵書與「剛毅」稽核(無代碼變更)

**分利**: 全庫(root data/*.json、docs/data/*.json、bingshu.json)逐字搜尋無「分利」
這個名稱的兵書/戰法/裝備/緣分條目。比對戰報線索「統率增加已達最大疊加層數」, 全庫
唯一符合「統率屬性可疊加且已有明確揭露疊層機制缺口」的條目是內政/指揮戰法
**權僭九鼎**(`docs/data/tactics_parsed.json`, INNATE/COMMAND, S)——原文「自身受治療時
50%機率+2.5→5點統率和智力, 可疊加99次」, 現行建模已誠實揭露(`_note`, 批28 A2):
「以單次期望近似, 實際應逐次觸發疊加(引擎無疊加計數器, 近似為持續加成)」。此為已知、
已誠實disclosure的近似缺口, 非隱藏bug——需要的是「受治療時反應式觸發」+「stat效果
真正逐次疊加計數器(含max上限)」兩個新機制的組合, 屬結構性缺口(同`stack`原語目前只
支援`amp`/`mitig`, 不支援`stat`累加), 規模超出本批查證範圍, 本批不動代碼, 僅確認
現況揭露誠實且無需修正。若user能指認「分利」實際對應的戰法/兵書名稱, 下批可針對性
處理。

**剛毅**: 全庫僅一處提及「剛毅」——虛實奇謀(戰報`data/tactics.json`)「每回合
25%→50%機率使我軍群體(2-3人)進入1回合剛毅狀態」, 現行建模用`insight`(免疫控制)
近似, `e.rate`已正確編碼為0.5(滿級值)。惟user戰報線索「-48.73%輸出減益+機率沒生效」
反解: 48.73/1.5=32.49——酷似某效果基礎值32.49%被`SCALE_CLAMP`(±1.5, 見引擎常數)
封頂命中的結果, 但全庫掃描找不到任何debuff基礎值≈32.49%的兵刃/謀略/兵書條目可以
對應。虛實奇謀本身的機率值(50%/35%)經比對與此數字無關聯。因缺乏可辨識的具體戰法/
兵書名稱與可信原始數值, 本批不猜測性修改任何現有條目, 判定證據不足以行動, 誠實記錄
待user補充具體戰法名稱/戰報截圖後再排查。

### 驗收

`node --check docs/engine.js` / `python sgz.py --selftest`(新增89c-2: ofDamage scale
兩點錨點506.18→68.6%/237.65→41.2%; 86b-2/86b-3: chargeOnly正確生效+activeOnly不再
誤觸charge傷害的迴歸防線) / node ad-hoc驗證腳本(對稱交叉核對ofDamage scale與chargeOnly
兩引擎行為一致) / `python reparse_effects.py`(草船借箭 corrections↔parsed 兩輪byte-diff
為空, 冪等) / `python lint_tactics.py --selftest`(69項全過) / `python lint_tactics.py`
(R1-R26全庫零違規, R20能力漂移偵測零缺口)。

## 39. 批41: v18零分修復(鷹視狼顧/錦帆軍) + R27主將身份揭露防線 + R28數值一致性抽查

**背景**: 盲測v18(79分)回報2筆0分戰法 + 2條防線建議, 本批純資料/lint修復, 不動
`docs/engine.js`/`sgz.py` 引擎邏輯。

### A. 兩筆0分修復

**鷹視狼顧**: 原文「自身為主將時，獲得8%→16%奇謀機率」明確為主將身份閘門條件, 但
`amp(who:self,val:0.16)` 效果缺 `ifLeader`(批26原語, 同庫西涼鐵騎/天下無雙/水淹七軍等
3筆precedent正確用法), 舊版對任何施放者(非主將亦然)無條件套用+16%常駐增傷, 高估
非主將持有情形。補 `ifLeader:true`。lifesteal效果(when:{from:5}母戰法t.when的
§6.4停損)未動, 僅修amp這段。

**錦帆軍**: 原文兩版本(2023-05-09更新版/原始版)皆一致寫「傷害率32%→64%」, 但
`coef` 卻是 `1.28`(64%的2倍)。根因: 批23 `_note` 誤把「當時parsed.coef是0.64」
本身當成bug敘述(稱其為「主段傷害被腰斬近半」), 反向宣稱1.28才是正確值並把coef改成
1.28——實際是敘事方向搞反, 1.28才是被誤加倍的錯誤值。本批訂正回 `coef:0.64`, 並
重寫 `_note` 記錄此教訓(避免未來再次被舊_note的錯誤敘事誤導)。核對其餘欄位:
`extraHits.coef:1.1` 對應「傷害率55%→110%」一致無誤(未動); `amp.val:0.06` 對應
「提高友軍3%→6%會心」數值一致(「若甘寧統領」持有者條件遺漏維持既有揭露, 非本次
coef敘事問題範圍)。

### B. 兩條新防線

**R27(主將身份揭露)**: 全庫掃描「自身為主將時/若自身為主將/自身若為主將/若為主將/
若我軍主將」等施放者身份條件措辭(40餘筆), 排查戰法/效果是否有 `ifLeader`/
`leaderBonus` 或提及「主將/leader」的揭露文字。初掃發現4筆真缺口:
- **一身是膽**「自身為主將時，基礎觸發機率提升至35%→70%」——機率縮放語意(ifLeader
  二元閘門無法表達, 同仁德載世R24既有precedent), 補 `_todo` 誠實揭露, 不勉強加欄位。
- **圍師必闕**「自身為主將時，謀略傷害降低值提升至22.5%→45%」——比照水淹七軍
  (批37 B)的「基礎段(無條件)+差額top-up段(ifLeader:true)」拆法, 新增第二條
  `mitig(val:0.06, ifLeader:true)` 與既有 `mitig(val:0.39)` 並行, 主將時
  0.39+0.06=0.45 對齊滿級值。
- **奉令平虜**：`effects:[]`(整段完全未建模, 批23已誠實揭露), 主將機率加成
  (20%→40%)本屬同一未建模範圍, 於既有 `_todo` 補一句明確提及, 無需新增欄位
  (effects為空無處可掛)。
- **火燒連營**：查證後為假陽性——`extraHits[0]._note` 已明確揭露「自身為主將時
  提高至70%...已直接取滿級主將值」, 只是 `_disclosure_texts()` 這個共用輔助函式
  過去只掃 `p`/`p["effects"]`, 遺漏 `p["extraHits"]`/`p["choices"]`(及其內部
  effects)的揭露文字, 是既有的盲點(影響所有倚賴 `_topic_disclosed(effect=None)`
  的規則, 非R27獨有)。已修復 `_disclosure_texts()` 補掃這兩類欄位(全庫核對17處
  受影響, 純粹擴大豁免範圍, 基準線本為0違規, 不會讓真違規變成不違規)。
- 其餘36筆核對後皆已有 `ifLeader`/`leaderBonus` 或戰法級「主將」揭露文字, 無需
  修改。

修復後全庫R27掃描: **0違規**。selftest含鷹視狼顧陽性回歸(修復前/後各一組)。

**R28(數值一致性抽查)**: 正則抓「傷害率X%→Y%」「治療率X%→Y%」宣告, 取滿級值Y與
對應coef(×100)比對, 偏差>5%且無揭露解釋則判定違規。**方法論取捨**: 初期試跑「宣告
順序↔coef欄位JSON順序」的位置配對在多段戰法(如水淹七軍第4段extraHits傷害
104%→208%誤配到ifLeader top-up dot)產生大量假陽性, 且「文字裡第一個傷害率宣告
=頂層coef」的95%準確捷徵法仍有7筆例外(其中十面埋伏/苦肉計/左右開弓屬地雷:
分別因版本區塊子句順序調換、自傷害非目標傷害、crit-EV隱性折算未揭露, 三者性質
互異且regex難以低成本精確排除)。依任務指示「假陽性寧缺勿濫」原則, 最終R28**只在
「宣告數==coef欄位數==1」(dmg類/heal類分軌各自判定)的簡單情形下比較**, 多段戰法
(如錦帆軍本身, 2個dmg段)保守跳過不判定——以「不誤報」換取「部分真違規需要多段
專屬工具才能自動抓」的已知覆蓋率犧牲(見R28程式碼註解的完整方法論記錄)。

初掃(全庫, 單段case): 10筆進入候選(承天靖世/神機妙算/垂心萬物/蕙質蘭心/解煩衛/
金城湯池/錦囊妙計/離月/斂眾而擊 + 錦帆軍修復前的模擬), 逐筆核對後**全部為既有
`_note`/`_todo` 已誠實解釋的EV折算或版本分支**(如錦帆軍amp的甘寧統領EV折算、
垂心萬物的INHERITANCE/INNATE同名重複條目版本誤用已知案例等), 無新增真違規。
修復後全庫R28掃描: **0違規**。selftest含錦帆軍陽性回歸(單段合成樣例, 修復前/後
各一組)+ heal案例+多段保守跳過案例。

多段戰法的數值一致性仍是**已知殘餘覆蓋率缺口**(如左右開弓top.coef疑似crit-EV
折算但無`_note`明確揭露這個折算來源, 本批未動, 留待未來若有专属工具或user提供
戰報實測再排查), 記錄於此供後續批次參考。

### C. 意外發現並修復: `ifLeader` top-up段與基礎段共用 `push_add` dedup key 互相覆蓋

**背景**: 為修圍師必闕的R27違規, 比照水淹七軍(批37 B)precedent, 在同一戰法內新增
第二條同 `k`("mitig")的效果(`val:0.06, ifLeader:true`), 與既有基礎段
(`val:0.39`)並存, 預期兩段相加=0.45(主將時)。TRACE驗證時發現主將施放者只拿到
0.06(遺失基礎段的0.39), 而非預期的0.45。

**根因**: `sgz.py`/`engine.js` 的 `amp`/`mitig` 效果透過 `push_add(kind, val, dur,
src, flags)` 掛載加成, dedup key 為 `(kind, src)`——同一戰法(同 `src`=戰法名)同一
`kind`(如都是`mitig`)且 `dmgType`/`normalOnly`/`activeOnly`/`chargeOnly` 尾碼也相同
時, 後套用的效果會把先套用的整個蓋掉(「同源刷新」設計, 見 `push_add` docstring)。
水淹七軍的base+top-up precedent之所以沒踩到這個坑, 是因為它用的是 `dot`(走
`u.dots.append`, 不去重, 天然可疊加), 而本次新增的是 `mitig`(走 `push_add`, 會
去重覆蓋)——**同一種「base+top-up」設計模式, 換一個效果種類(`k`)就會因為底層
存放結構不同而產生完全不同的行為**, 是本次修復過程中才發現的隱性地雷。

**修法**: 比照既有 `leaderBonus`(chargeup專屬)的 `":leaderBonus"` 尾碼慣例(見本檔
第20節/`engine.js` k==="chargeup"分支), 為 `ifLeader:true` 的 `amp`/`mitig` 效果
的 `dt_src`/`dtSrc` 補上 `":ifLeader"` 尾碼, 使其與同戰法內無 `ifLeader` 的基礎段
產生不同的 dedup key, 兩段各自獨立掛載、疊加而非覆蓋。`sgz.py`/`docs/engine.js`
對稱修改(呼叫端結構完全一致, 修法逐行對應)。

**影響範圍**: 全庫掃描確認此為**圍師必闕獨有**的新組合(唯一「同 `who`+同
`dmgType` 的兩條 `amp`/`mitig`」的戰法, 見批41任務排查), 修法前不影響任何既有
戰法的既有行為(既有的鷹視狼顧/江東小霸王/酒池肉林等 `ifLeader` 用法皆是「單一
`amp`效果+ifLeader」, 沒有base+top-up的第二段可覆蓋)。修法本身只在
`e.get("ifLeader")` 為真時才附加尾碼, 對所有既有無 `ifLeader` 的效果零影響
(向後相容)。

**迴歸測試**: `sgz.py` 自我檢查新增112(圍師必闕: 主將mitig(intel)應比非主將多
0.06, 而非兩段互相覆蓋只剩其中一段)/113(鷹視狼顧: 非主將施放者不吃16%奇謀增傷,
v18零分修復的TRACE回歸)。

### 驗收

`node --check docs/engine.js` / `python -c "import py_compile;
py_compile.compile('sgz.py', doraise=True)"` / `python reparse_effects.py`(兩輪
byte-diff為空, 冪等) / `python lint_tactics.py --selftest`(78項全過, 含R27/R28
共9個新樣例) / `python lint_tactics.py`(R1-R28全庫零違規) / `python sgz.py test`
(自我檢查含新增112/113兩項迴歸斷言全過) / TRACE抽驗鷹視狼顧非主將施放者不吃16%
奇謀增傷、主將施放者才吃; 圍師必闕主將施放者mitig(intel)=非主將值+0.06(兩段
疊加而非覆蓋)。

## 40. 批42: 傲睨王侯官方卡重建 —— `who:"eventTarget"`(事件單位精確選標) +
`k:"stat"` per-target 疊層原語(`stackKey`/`perStack`/`maxStacks`/`onMaxStacks`/
`globalMax`/`globalEffects`)

**背景**: user 提供傲睨王侯官方卡全文+兩點白字標智力戰報(許攸智力328.36→每層
4.78%/428.14→每層5.56%, 兩位小數精確吻合, 見 `calibration_anchors.json`
`aoni_wanghou_20260707`), 獨立解出第五條「受智力影響」曲線 `scaleDiv:385`(≠傷害
350≠狀態375≠發動率獨立常數≠此前草船統率曲線266)。舊版(批34前)用「ramp-avg固定
均值-15%全體敵軍開場套用」近似, 完全不反映「敵軍目標受普攻時才觸發, 逐層疊加在
那一個特定目標身上」的真實逐層動態機制, 也不反映兩段閾值(單目標5層全觸發/全場
15層觸發)各自精確的觸發時點。

**機制原文**: 15個破綻池分布全體敵軍(≈每敵5個)/敵軍目標受普攻時觸發1個破綻/
每層該目標全屬性(武智統速, 不含魅力)-3%×(1+(持有者智力-100)/385)可疊/單目標
破綻全觸發(5層)→該目標1回合虛弱+受傷提高15%(受智力影響)持續2回合/全場15個
破綻皆觸發後→敵軍群體2人全屬性-20%(受智力影響)。

### A. `who:"eventTarget"`(效果級選標值, 精確鎖定跨單位事件廣播的事件單位本身)

**缺口**: 批38新增的 `when.who:"ally"/"otherAlly"/"enemy"` 跨單位事件廣播只解決
了「持有者(可能不同於事件單位)是否該觸發」的問題, 觸發後效果實際套用的目標仍是
既有 `who:"enemy"/"ally"` 的「泛用該陣營全體/隨機N人」邏輯(如虎侯 `who:"ally"`
廣播監聽「任一友軍受擊」, 但效果套用時 `who:"ally"` 選標打的是「我軍全體」而非
「受擊的那一個友軍」) —— 無法表達「敵軍目標受普攻時, 觸發的效果只作用在被打的
那一個目標身上」這種精確 1:1 綁定語意。

**設計**: `applyEffects`/`apply_effects` 新增 `opt.evtTarget`/`evt_target` 參數
(對稱既有 `opt.dmg`/`dmg` 尾端新增慣例), 由 `onHitFor`/`on_hit_for`(廣播事件迴圈)
在呼叫端傳入「本次事件的事件單位本身」(即 `dst`, 受擊/受傷的那個單位); 效果級
`who` 新增第五個值 `"eventTarget"`, 解析為 `dests = [opt.evtTarget]`(其 alive 時)。
未傳 `evtTarget` 的既有呼叫路徑(prep/active/charge等), `who:"eventTarget"` 會落
空回傳 `[]`, 對全庫既有資料零回歸(只有新資料明確用 `who:"eventTarget"` 才會用到)。

### B. `k:"stat"` per-target 疊層原語(`e.stackKey`/`e.perStack`/`e.maxStacks`)

**缺口**: 批38虎侯遷移時已點名「stat原語本身不支援疊層計數」(`pushAdd`/
`pushMod`/`pushStatAdd` 皆是「同來源刷新覆蓋」語意, 非疊加), 對比 `block` 的
`times`/`stack` 的 `per`+`max` 才有疊層語意, 但兩者都是「掛在持有者/目標自身
單一狀態物件上」的計數, 不支援「持有者對多個不同敵方目標各自獨立疊層計數」這種
per-(效果,目標) 二維計數語意。

**設計**: `k=="stat"` 新增 `e.stackKey`(truthy 旗標, 標記走疊層模式而非既有
`add`/`mult` 二選一單次套用) 分支。疊層計數 `exploitLayers`/`exploit_layers`
(`Map`/`dict`, 鍵=效果物件本身, 惰性建立)掛在**目標**(受害者)身上而非持有者
——因為疊層語意是「敵人身上累積的破綻層數」, 不同敵人各自的 `exploitLayers`
是各自 Unit 實例上獨立的 Map, 天然互不干擾, 不需要額外的目標維度索引。每次
觸發: 若已達 `e.maxStacks` 則直接 `continue`(本地池已耗盡, 不再刷新/計入任何
東西, 語意上「無破綻可觸發」而非「持續疊加但封頂」); 否則層數 `+1`, 用
`lockedScaleOf`(批35「準備階段鎖定」慣例, 佐證見 `aoni_wanghou` 錨點 laws
「prep鎖定再證」)算持有者智力縮放值, 重新計算「當前層數×每層量級
(`e.perStack`, 預設0.03)」的總乘數並 `pushMod`(同 `src`+`stat` 刷新覆蓋既有
慣例, 因新值已包含新層數, 天然等同疊加, 不需要另外的疊加型 `pushXxx` 原語)。

**雙閾值觸發**(`e.onMaxStacks`/`e.globalMax`+`e.globalEffects`, 皆為效果陣列選填):
- `onMaxStacks`: 該目標本地池首次耗盡(層數達 `maxStacks`)時額外套用的一次性
  效果段(如「該目標1回合虛弱+受傷提高15%持續2回合」), 用 `exploitCapped`/
  `exploit_capped`(`Set`, 鍵=效果物件)去重, 確保同一目標只觸發一次。遞迴呼叫
  `applyEffects` 時 `caster` 仍傳原持有者(scale 縮放基準人物不變, 「受智力
  影響」指的是持有者的智力), 目標則靠 `who:"eventTarget"`+`evtTarget:u` 精確
  指定——不能用 `who:"self"`(那需要 `caster=u`, 但换成 `u` 會連帶讓 scale
  誤用目標智力), 兩者互斥, 故用 `eventTarget` 機制解耦「持有者(scale基準)」
  與「效果套用目標」這兩個不同角色。
- `globalMax`/`globalEffects`: 持有者視角跨目標累計觸發次數(`exploitGlobal`/
  `exploit_global`, 掛在持有者身上, 鍵=效果物件, 值=`{n, fired}`), 每次「新層
  觸發」(非本地池已耗盡的重複刷新)才 `+1`, 達到 `globalMax`(原文「場上所有
  破綻」15個)且尚未觸發過時套用 `globalEffects`(如「敵軍隨機2人全屬性-20%」),
  `fired` 旗標防重複觸發(一次性語意)。

### C. 落地: 傲睨王侯全面重建(取代 ramp-avg 近似)

改用上述原語精確重建: 主效果 `who:"eventTarget"`+`stackKey`+`perStack:0.03`+
`maxStacks:5`+`scale:"intel"`+`scaleDiv:385`(user 兩點實測定案曲線)+
`when:{on:"attacked",who:"enemy"}`(批38廣播機制, 敵軍目標受普攻才觸發);
`onMaxStacks` 掛虛弱(`amp val:-1.0`, 既有克敵制勝/威謀靡亢/臨戰先登慣例)+易傷
(`mitig val:-0.15`, 既有負值=易傷慣例); `globalMax:15`+`globalEffects` 掛
`stat who:"enemy" n:2 mult:0.8`。`maxStacks:5` 為 15 個破綻均攤 3 人隊(每敵5個)
的近似值, 官方未明說確切分配規則, 若隊伍非3人/有陣亡等邊界情形分配未知, 已於
`_todo` 揭露。`onMaxStacks`/`globalEffects` 兩段的 intel scale 曲線族(350 vs
375)未經實測樣本裁決(`aoni_wanghou` 錨點只涵蓋主效果每層-3%這一段), 沿用全域
350 預設並保留揭露, 不擅自套 385/375。

**R20 alias 維護**: `ENGINE_CAPABILITY_ALIASES` 新增 `"事件單位本身"`(對應
`eventTarget`)與 `"stat原語完全無疊層計數能力"`(對應 `stackKey` 系列, 用完整
精確片語而非通用詞「疊加5次」/「破綻」當別名, 避免誤傷虎侯等「仍有其他獨立
缺口」的正確措辭——虎侯要的是「+15點固定值疊加5次」的 `add` 疊層形態, 與本批
只解決的 `mult` 百分比疊層形態不對應, 該限制對虎侯依然真實存在, 只是措辭改寫
為精確版本以避免 R20 誤判整段為 stale)。

### 驗收

`node --check docs/engine.js` / `python -c "import py_compile;
py_compile.compile('sgz.py', doraise=True)"` / `python reparse_effects.py`(兩輪
byte-diff為空, 冪等) / `python lint_tactics.py`(R1-R28全庫零違規) /
`python lint_tactics.py --selftest`(78項全過) / `python sgz.py demo`(自我檢查
新增114-117四項斷言: 兩點曲線錨點精確吻合/per-target疊層+`eventTarget`精確選標
只影響事件單位+5層封頂/`onMaxStacks`虛弱+易傷/`globalMax`全場15層觸發2人-20%,
全過) / `node scratchpad/smoke_batch42.js`(對稱10項斷言全過, 修復途中發現並
修正一處 `engine.js` 殘留的 `capped` 未定義變數參照——重構成 `continue` 早退
後遺留的舊變數引用, 只有跑到 `globalMax` 分支的路徑才會觸發, 端到端 smoke test
抓到) / `SGZ.trace` 端到端3v3實戰模擬50場無崩潰, TRACE 抽驗確認「破綻 第N層」
逐層遞增、僅普攻觸發(繳械/震懾回合無普攻不誤觸發)、`onMaxStacks`(「破綻全
觸發」)在300場內可觀察到; `globalMax`(「破綻全場觸發」)在標準8回合3v3規模下
偏難自然觸發(15層門檻對3人隊已是本地池上限總和, 需極高比例普攻集中命中同批
少數目標且無人陣亡, 3000場模擬仍未見, 屬遊戲平衡特性而非引擎缺陷, 已用獨立
`applyEffects` 單元測試在 sgz.py demo 117/smoke_batch42 直接驗證觸發邏輯本身
正確)。
