# 三國志戰略版 · 配將模擬器

**線上使用 → https://easychiu.github.io/sgs-pei-jiang/**

台服武將配將評分 + 逐回合模擬對戰 + 自動推薦,193 武將卡片圖鑑。
純靜態網站,瀏覽器內運算,無後端。**非官方社群工具,免費使用。**

## 功能

- **配將模擬**：選兩隊(各最多 3 將)+ 兵種 + 兵書/裝備 → 模擬 3000 場出勝率
- **自動推薦**：依勢力篩選,列出評分最高的陣容(含建議兵種)
- **武將圖鑑**：193 張華麗中國風卡片,點開看六維、五兵種適性、戰法白話說明

## 戰鬥模型

傷害公式(社群拆解模型 + 實測錨點校準,錨點見 `docs/data/calibration_anchors.json`):
```
傷害 = max(4.76×√兵力 + 1.44×(攻-防), 0.9×√兵力) × 戰法係數
       × 兵種克制 × 士氣係數(0.3+0.007×min(士氣,100)) × 增傷 × (1-減傷) × random(0.96~1.04)
```
- **屬性管線**(對齊遊戲詳細戰報順序): (基礎+加點)×兵種適性 → +城建20 → ×陣營1.10 → +兵種營4 → 裝備平加
- **兵種由「隊伍」決定**(玩家選),各武將只有對該兵種的**適性 S/A/B/C/D** 決定屬性發揮(×120/100/85/70/55%)
- **克制是隊伍兵種 vs 隊伍兵種**:騎>盾>弓>槍>騎(器械全被克),克制 ×1.15 / 被克 ×0.85
- 逐回合、速度排序(同速隨機);主動/突擊依真實發動率,指揮/被動/陣法/兵種開戰依序生效,支援回合窗與受擊觸發
- **戰法原語 24 種**:增傷/減傷/震懾/計窮/繳械/洞察/先攻/治療/屬性/DoT/結算(猛毒)/連擊/代承/疊加/衰減/武智互換/無視防禦/反擊/嘲諷/護盾/規避/必中/發動率(rateup)/突擊率(chargeup)
- **受屬性影響縮放**: 效果量隨戰鬥內即時屬性縮放(全域 1/350;發動率類實測 0.0026 獨立常數)
- **兵書**(47) · **緣分**(20):隊伍湊齊觸發 · **特技裝備**(72,四欄×雙特技,同名僅一件生效)

## 資料

| 內容 | 量 | 說明 |
|------|----|------|
| 武將 | 193 | 勢力/性別/六維+成長/五兵種適性/自帶戰法/可用兵書 |
| 戰法 | 384 | `data/tactics.json` 全文庫 effectText → 引擎原語編碼（`docs/data/tactics_parsed.json`） |

**本文回寫（必須）**: 定稿中文必須回寫 `data/tactics.json` 方便 diff。執行 `python sync_tactics_effecttext.py`（來源: overrides → corrections.effectText → 合格 `_evidence`）。
| 兵書 | 47 | 作戰/虛實/軍形/九變 × 主/副 |
| 緣分 | 20 | 如桃園結義(劉關張) |
| 裝備 | 35 | 武器/防具/坐騎… |

數值為遊戲客觀事實,戰法/兵書/緣分/裝備效果以本專案自有原語重新編碼。

## 已知近似(精度天花板)

引擎以**技能原文為真相**對齊,結構性缺口列於 `docs/data/engine_limitations.md`:
- 治療已支援 who/e.n/群體/自身/主將/急救 eventTarget(批52);其餘如冷卻、真疊加、主將數值分支仍待補
- 條件/多模式戰法部分仍近似;指揮/被動傷害可用觸發折扣旋鈕(`CMD_TRIGGER`/`PASSIVE_TRIGGER`)
- 部分戰法數值經考據修正,帶 `_src`/`_real`/`_conf` 註記;`_conf=med` 以官方面板為準
- 武將取 Lv50、雙方同級(等級壓制略過);士氣固定滿
- 改資料不必動引擎,效果原語都在 JSON

## 開發

網站(`docs/`)純 vanilla JS,無 build step;`docs/engine.js` 是 `sgz.py` 的 JS 移植,兩版結果一致。

```bash
python sgz.py                              # demo: 推薦 + 一場模擬
python sgz.py sim "呂布,趙雲,關羽" "諸葛亮,周瑜,司馬懿" [兵種A 兵種B]
python sgz.py rec [勢力]                   # 配將推薦
python sgz.py info 呂布                     # 武將面板 + 五兵種適性 + 戰法解析
python sgz.py test                         # 自檢
```
Windows 主控台中文亂碼加 `PYTHONUTF8=1`。本地預覽網站:`python -m http.server --directory docs`。
